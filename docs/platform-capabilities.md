**English** | [中文](platform-capabilities.zh.md)

# Platform capability matrix

- Upstream: MediaCrawler `d6f7c5bb906b6dac40ddf343ef9e26438a3de092`
- Meaning: ✅ reachable implementation
- Qualification note: Symbols describe source reachability, not live account qualification; all live rows remain `NOT_RUN` unless explicitly recorded otherwise.

## Login paths

| Platform | QR | Cookie | Phone in source | Phone through main CLI | Saved session |
| --- | ---: | ---: | ---: | ---: | ---: |
| Xiaohongshu | ✅ | ✅ | ⚠ implemented | ❌ core passes empty phone | ✅ |
| Douyin | ✅ | ✅ | ⚠ implemented with SMS cache and slider caveats | ❌ core passes empty phone | ✅ |
| Kuaishou | ✅ | ✅ | ❌ `pass` | ❌ | ✅ |
| Bilibili | ✅ | ✅ | ❌ `pass` | ❌ | ✅ |
| Weibo | ✅ | ✅ | ❌ `pass` | ❌ | ✅ |
| Tieba | ✅ | ✅ | ❌ `pass` | ❌ | ✅ |
| Zhihu | ✅ | ✅ | ❌ TODO | ❌ | ✅ |

Evidence: login enum at `cmd_arg/arg.py:52-57`; XHS call site `media_platform/xhs/core.py:103-113` and implementation `media_platform/xhs/login.py:87-224`; Douyin call site `media_platform/douyin/core.py:100-109` and implementation `media_platform/douyin/login.py:53-89,124-169,266-274`; placeholder implementations in each remaining `media_platform/*/login.py`. The upstream WebUI itself exposes only QR and Cookie (`api/main.py:166-187`).

### media-sync 0.x exposure

The execution 0012 worktree exposes an explicit, blocking QR-login command for one eligible initial MediaCrawler QR account or one exact `saved_session/expired` account across all seven platform identifiers. Both `--enable-mediacrawler` and `--accept-mediacrawler-license` are required before settings, database or child work. The isolated login-only child forces a headed browser and saved state; reauthentication start atomically becomes `qr/authenticating`, successful durable handoff changes the account to derived per-account `saved_session/authenticated`, and non-success leaves a retryable QR state. The child remains under START/CANCEL/EOF parent control and a post-result guardian until complete-tree shutdown. Cookie remains a non-interactive secret-reference path, saved sessions are background/headless outside this explicit reauthentication flow, and phone is **not** exposed. This intentionally differs from the overly broad upstream enum.

The focused offline gate proves the closed seven-identifier protocol, state transitions, process-tree join and redaction behavior; it does not prove a real QR render or login. Every live row remains `NOT_RUN`, and phone remains unsupported rather than untested.

## Creator/content behavior

| Platform | Creator reference | Creator content | Upstream cap behavior | Profile persisted |
| --- | --- | --- | --- | ---: |
| `xhs` | 24-char ID or profile URL; token parameters may be required | Image/video notes | ✅ page loop checks maximum | ❌ |
| `dy` | `sec_user_id` or `/user/...` | Image/video aweme | ❌ traverses until `has_more=0` | ❌ |
| `ks` | user ID or `/profile/...` | Video posts | ❌ traverses until `no_more` | ❌ |
| `bili` | UID or space URL | Creator videos | ❌ full history, 30 per page | ❌ |
| `wb` | numeric user ID | Weibo notes | ❌ full mobile-container pagination | ❌ |
| `tieba` | home URL; CLI also accepts portrait ID | Author threads | ✅ upstream checks the maximum; execution 0020 shim enforces exact successful `max_items` work | ❌ |
| `zhihu` | `/people/<url_token>` | Answers only by default; article/video calls disabled | ❌ pinned loop ignores cap; ✅ execution 0019 shim enforces Subscription `max_items` | ❌ |

Creator-mode dispatch exists for all seven platforms (`media_platform/*/core.py:120-142`). The CLI routes `--creator_id` into six platform lists but omits Zhihu (`cmd_arg/arg.py:388-402`). Most creator stores are deliberately no-ops and content uses an anonymized creator hash (`tools/user_hash.py:11-36`; `store/{xhs,douyin,kuaishou,bilibili,weibo,tieba}/__init__.py`). Zhihu's creator core does not call a creator store, and its JSONL `store_creator` is also a no-op, so no platform in this bridge provides a trustworthy creator profile row.

### Bridge policy

- Preserve the user-supplied remote creator ID and a user-provided display label in the independent `media-sync` database.
- Give every run a hard wall-clock timeout and output-item watchdog.
- Require an explicit `allow_full_history` acknowledgement only for an audited creator path that remains unbounded. Executions 0019 and 0020 install verified bounded Zhihu-answer and Tieba-thread loops; Zhihu is removed from that acknowledgement set, while Tieba already had an upstream maximum check and is now hardened to exact successful work.
- Stop incremental ingestion at known IDs/publish watermark even if the child emitted older records; never treat downstream truncation as proof that upstream traffic was bounded.
- Work around Zhihu creator input in the external runner without editing the upstream checkout.

## Media behavior

| Platform | Metadata | Upstream binary download | Qualification |
| --- | ---: | --- | --- |
| `xhs` | ✅ | Images and video | ⚠ full response in memory, no resume/checksum |
| `dy` | ✅ | Images and video | ⚠ same limitations |
| `ks` | ✅ | ❌ URL only | Requires media-sync downloader |
| `bili` | ✅ | ⚠ upstream still downloads only the first CID and one progressive URL | ✅ executions 0023–0027 capture 1–64 page/CID identities, qualify compatible progressive/DASH Assets, add ordered primary/backup failover and remux an explicitly declared exactly-one-segment FLV to MP4 with bounded ffmpeg; multiple `durl` segments/FLV concatenation or transcoding, CDN ranking/racing/cross-run cache, pages above 64, subtitles and danmaku remain deferred |
| `wb` | ✅ | ⚠ images only and the creator path still does not call the upstream downloader | ✅ execution 0016 captures a closed static-image locator shape into normalized IMAGE/GALLERY Assets |
| `tieba` | ✅ | ❌ | ✅ executions 0020–0022 capture compatible one-image, exact-two-image and bounded 3–64-image ordinary first-floor ARTICLE rows; galleries above 64 and mixed/rich/reply media remain deferred |
| `zhihu` | ⚠ answers by default | ❌ | ✅ execution 0019 captures exactly one ordinary static answer image into ARTICLE + IMAGE; multiple images/articles/zvideo remain deferred |

Media download is disabled by the misspelled non-CLI switch `ENABLE_GET_MEIDAS` (`config/base_config.py:107-108`). Implementations are under `store/*/*_store_media.py`; current HTTP clients buffer complete responses and lack `.part`, Range resume, MIME/probe and checksum validation.

### media-sync downloader/export status

Execution 0005 implements an offline-qualified, platform-independent downloader and Emby/Jellyfin layout v1. Query-free `direct` locators use per-hop public-DNS validation, address-pinned connections, manual redirects, strict resumable Range semantics, byte/time limits, MIME/container probing, mandatory bounded `ffprobe` structural validation for video/audio, SHA-256 and immutable content-addressed publication. Download orchestration adds a per-asset OS lock, a non-disclosing work/archive scope fingerprint, exact lease/reclaim CAS and restart recovery after archive commit but before database finalization. In 0.x, these filesystem guarantees assume dedicated operator-controlled runtime roots and ancestors; hostile same-permission parent-directory substitution is outside the threat model.

Export uses deterministic creator/content identities, NFO and allowlisted provenance, an author lock, staging and a filesystem manifest/file CAS. Managed ownership does not come from the disk manifest alone: succeeded `export.emby` Job results form a unique predecessor chain and anchor exact source/tree/manifest hashes. Publication and interrupted roll-forward revalidate the complete desired managed tree before success or journal cleanup. Pre-publish intent supports exact database-finalization recovery, including empty snapshots; `A → B → A` is valid, a forged or unexpected manifest is rejected, and concurrent siblings leave one winner without deleting user-modified or unmanaged files.

MediaCrawler-discovered assets intentionally persist only a stable `adapter_refresh` locator because platform/CDN URLs may contain expiring signatures. Execution 0009 commit `98cf387` implements a default-off lazy refresh path bound to the exact current Asset/Subscription observation. `asset download` requires both `--enable-mediacrawler` and `--accept-mediacrawler-license`, plus `--subscription-id` when an exact source must be selected; XHS additionally accepts one ephemeral `--xhs-detail-reference-ref` for the exact note URL. Refreshed signed URLs remain in the private result/memory/HTTP boundary and are not written back to SQLite.

Execution 0017 implementation `2f8dbaa`, planned in checkpoint `9d19e7e`, closes the current automatic XHS author-subscription path without rewriting the historical execution 0009 boundary. An explicit ephemeral `xhs_detail_reference_ref` remains the compatibility override. Otherwise, refresh follows the selected exact `AssetRefreshSource` to its exact Subscription and privately resolves only `policy.mediacrawler.creator_input.secret_ref`. The signed creator URL is validated against the trusted XHS author, host and path, and the creator run is bounded by that Subscription's `max_items`. Creator and detail authority are mutually exclusive; the refresh context, parent request and child loader independently require the matching author ID and one unique non-empty `xsec_token`/`xsec_source`. Multiple returned JSONL records are still selected by exact content ID, Asset kind/position and source hint; no database migration or durable per-note authority was added.

At the execution 0017 historical boundary, the offline composition qualifies only ordinary `type="normal"` XHS notes with one or more ordered static images as IMAGE/GALLERY. Controlled image bytes traverse `MediaRequestProfile.DEFAULT` without Cookie, Authorization, Referer or Origin, immutable SHA-256 archives and idempotent Emby/Jellyfin poster/backdrop/gallery/NFO/source publication; replay adds no detail, HTTP, archive or export work. Durable raw, SQLite, archive metadata and exported output contain neither creator/note authority nor signed-media query values. At that boundary, automatic XHS video, live photo, animation, mixed media and authority-expiry recovery remained unimplemented or deferred. Real login, creator/feed/detail traffic, XHS CDN bytes and Emby/Jellyfin server scan/playback remained `NOT_RUN`.

Execution 0018 implementation `356e254`, planned in checkpoint `c9d3586`, is the successor qualification for one ordinary automatic XHS `type="video"` row. Its raw scalar `video_url` must contain exactly one candidate and raw scalar `image_list` zero or one candidate, mapping one-to-one to one position-zero VIDEO plus optional IMAGE. Video-only is VIDEO; one artwork plus one video is the narrow qualified MIXED shape. The initial URL must be an ordinary bounded HTTP/HTTPS XHS CDN host with strict labels, default port and non-root path; malformed/foreign/userinfo/fragment/multiple/container-drift values fail closed, while redirects retain the downloader's per-hop public-network policy. `MediaRequestProfile.DEFAULT`, a source-bound pinned-store contract, a real fake checkout, independent production `FFprobeMediaProbe` validation of an embedded H.264 MP4, deterministic mock composition, SHA-256 archive, Emby `.mp4`/poster/NFO/source output and zero-work query replay pass. Multi-video, multi-image, broader mixed-media, live-photo, animation and authority-expiry recovery remain deferred. Real login, creator/feed/detail, CDN bytes and Emby/Jellyfin scan/playback remain `NOT_RUN`.

Execution 0019 implementation `2edb9d7`, after plan checkpoint `dc1714c`, adds one ordinary Zhihu creator-answer IMAGE shape and makes Zhihu the sixth platform with focused offline media output. The verified-checkout shim captures an image before the pinned extractor/store path discards its attributes, binds capture to the exact returned Pydantic model and uses `ContextVar` only inside nested storage. Gather-child → parent-store and non-serialization regressions pass. Attribute selection follows `data-original` → `data-actualsrc` → `src`; competing `srcset`, `data-src` or lazy candidates, duplicate selected attributes, multiple images and playable/container drift fail closed. Canonical answer/image URL gates include empty delimiter rejection.

The scheduled creator child passes manifest/Subscription `max_items` to a bounded answer loop and validates short/repeated/malformed pages. The end-to-end proof maps 23 to two API requests and two callback invocations with page sizes `20 + 3`, exactly 23 callback-processed rows and one between-page pacing sleep; there is no third request or post-cap sleep. Zhihu is removed from `FULL_HISTORY_PLATFORMS`, so `allow_full_history=false` is valid. Normalization keeps ARTICLE plus one `<content_id>:image:0` IMAGE, recursively strips private/transient authority and persists only a query-free hint. Exact canonical authority drives credential-free DEFAULT-profile refresh. Zhihu IMAGE automatically enables bounded static structural qualification: qualified JPEG/PNG/WebP pass, GIF/APNG/animated WebP/AVIF fail, and normal/recovery/takeover paths preserve the flag. This is not complete pixel decoding. The final focused gate passes 505 tests, the complete suite passes 1543 with one Windows-inapplicable skip, all quality/audit gates pass, and independent review finds no P0/P1/P2. No real redacted fixture exists and all live rows remain `NOT_RUN`.

Execution 0020 implementation `8a0e935`, after plan checkpoint `df7a38a`, adds the seventh platform's first downloadable media slice without an upstream edit or migration. The verified-checkout Tieba shim captures exactly one current integer type-3 image from ordinary first-floor `content`, binds it to the exact returned model across gather-child → parent-store, and uses `ContextVar` only during nested JSONL storage. The gate requires ordinary type-0 text siblings plus the exact current ten-key image object, selects only a signed HTTPS `tiebapic.baidu.com/forum/pic/item/<40-hex>.<jpg|jpeg|png|webp>?tbpicau=...` `origin_src`, and rejects zero/multiple/other/drift shapes for this media claim. Scheduled `max_items=23` becomes `20 + 3` detail/callback batches with no third page or post-cap sleep.

Normalization keeps ARTICLE plus one `<note_id>:image:0` IMAGE and persists only the query-free scheme/authority/path. Lazy refresh derives exact non-secret authority from the canonical SQLite thread URL, sends the numeric ID through `TIEBA_SPECIFIED_ID_LIST`, requires one matching ARTICLE/IMAGE/hint and returns only a newly validated signed URL under credential-free DEFAULT profile. Tieba IMAGE automatically uses the same bounded structural gate: qualified JPEG/PNG/WebP pass, while GIF/APNG/animated WebP/AVIF fail; normal, recovery and takeover preparation preserve the flag. The deterministic SQLite → fake detail → mock public DNS/HTTP → SHA-256 archive → Emby poster/backdrop/gallery/body/NFO/source composition and query-only zero-work replay pass without retaining private fields or `tbpicau`. The focused regression passes 368 tests and the complete suite passes 1650 with one Windows-inapplicable skip; all quality/build/upstream/audit gates pass. Real login/creator/detail/CDN and Emby/Jellyfin server rows remain `NOT_RUN`; first-floor galleries and all other Tieba media/rich-content types remain deferred.

Execution 0021 implementation `e0fb8d5`, planned in `5095ed6`, preserves that v1 single-image branch and adds a separate exact-two-image v2 capture. Both type-3 items must satisfy the same frozen structure and have distinct query-free identities; source order becomes positions 0/1. Dual fields, duplicate identity, three-or-more images and other content types fail closed. The refresh context freezes the complete persisted one- or two-image tuple, so either double-image position requires the same complete current ordered gallery; missing, reordered or replaced identities fail before a signed locator is returned. Two credential-free DEFAULT-profile downloads pass production JPEG/PNG static qualification, publish two SHA-256 archives and render Emby poster/backdrop/two gallery files/body/NFO/source. Query-only replay adds no work and whole-tree assertions retain no private field or `tbpicau`. Focused `413 passed`; complete `1668 passed, 1 skipped`; all quality/build/docs/upstream/audit gates pass. Three-or-more/mixed/rich/reply media and all live rows remain outside the delivered boundary.

Execution 0013 implementation `dd6cfec` extends the current Bilibili shape from cover-only to one offline-qualified ordinary numeric-aid video: a stable locator-only `source_url=NULL` Asset at `<aid>:video:0`, exact first-CID lookup and exactly one progressive `durl`. A closed Bilibili media profile supplies fixed browser-like UA, Referer and Origin while rejecting Cookie, Authorization and arbitrary caller headers. The signed URL remains ephemeral across detail, refresh, redirect/resume and one 401/403 re-resolution; synthetic MP4 bytes reach controlled probing, archive finalization and idempotent Emby primary-media publication.

At the execution 0013 historical boundary, durable identity did not contain CID because forward JSONL lacked it. A later first-CID replacement under the same aid therefore could not automatically bump generation or invalidate already-verified bytes. Execution 0023 now adds page/CID capture and CID-bound identities for compatible multipart uploads while preserving `<aid>:video:0` for exact single-page compatibility.

Execution 0023 implementation `24fd41c`, planned in `bd45478`, captures a canonical task-local 1–64-page tuple before the pinned Bilibili store drops `View.pages`. Malformed, non-contiguous, duplicate-CID and 65-page declarations fail closed. Qualifying 2–64-page uploads emit ordered locator-only `<aid>:video:cid:<cid>` VIDEO Assets. Detail protocol v4 sends only the target `bili_video_cid`, accepts exactly one progressive `durl` for that CID and returns the complete current tuple in memory; lazy refresh requires exact sibling size/order/CID/position/remote-ID equality before URL return. Private page/play fields and signed URLs are recursively absent from retained state. Three distinct mock byte streams traverse targeted detail, fixed Bilibili media profiles, DNS/HTTP, controlled probing, SHA-256 archives and deterministic Emby primary/two-part/NFO/source publication; query-only replay adds zero work. Focused `436 passed in 53.96s`; complete `1739 passed, 1 skipped in 321.25s`; all quality/build/docs/upstream/audit gates pass. DASH audio/video mux, FLV remux, multiple `durl` segments, subtitles, danmaku, backup failover, pages above 64 and bangumi/paid/live media remain unsupported or deferred; all real login/API/CDN/Emby/Jellyfin rows remain `NOT_RUN`.

Execution 0024 implementation `12314b9`, planned in `a7d038e`, upgrades the strict detail protocol to v5. The exact target CID is requested through WBI `/x/player/wbi/playurl` with `qn=127`, `fourk=1`, `fnval=4048` and `platform=pc`. A closed selector takes the highest supported DASH video quality, prefers AVC → HEV → AV1 at equal quality, chooses audio through the pinned ordinary/Dolby/Hi-Res ordering and accepts a valid silent-video shape. Signed primary, backup and component URLs are repr-safe and remain ephemeral. Generation-scoped video/audio stores provide strict Range resume, per-component probes, a combined byte cap, a fixed bounded `ffmpeg -c copy` mux and final `ffprobe`; only the validated final is published to the immutable SHA-256 archive. Failed muxes retain verified components, and a prepared published final recovers without another detail/DNS/HTTP/ffmpeg call. Real local H.264 and AAC components traverse production ffprobe/ffmpeg and produce an Emby MP4 with both streams. Focused `456 passed in 66.47s`; complete `1780 passed, 1 skipped in 333.43s`; all quality/build/docs/upstream/audit gates pass. Backup URLs are validated and modeled, but failover is not implemented; FLV, segmented `durl`, subtitles, danmaku, pages above 64 and broader Bilibili media remain unsupported or deferred. Every real login/API/CDN/Emby/Jellyfin row remains `NOT_RUN`.

Execution 0025 implementation `fe45abc`, planned in `8e9467d`, turns the already-validated ephemeral DASH candidate lists into independent ordered passes for video and optional audio. Each pass uses primary plus at most eight distinct backups under the existing asset lock, component/combined byte limits and shared deadline; a successful primary touches no backup DNS or HTTP. Candidate-local DNS, timeout, transport, interruption, HTTP-status and partial-Range incompatibility failures may advance. Forbidden or mixed network addresses, redirect/header/encoding, chunk/size, filesystem, probe and mux failures remain immediate. Cross-candidate append requires exact Range offset, total length and validator type/value continuity; mixed candidate failures preserve the partial, and bounded discard/restart occurs only after the complete pass rejects it. Exhausting only `401`/`403` preserves `locator_refresh_auth_expired`; retained SQLite, Job, runtime, work, archive, export and error state contains no candidate URL, host or winning index. A production-process composition makes the video primary return `503` and audio primary return `403`, reaches each backup independently, then passes ffprobe → ffmpeg → final ffprobe → SHA-256 archive → Emby dual-stream publication and zero-work replay. Focused `466 passed in 66.96s`; complete `1790 passed, 1 skipped in 331.33s`; all quality/build/docs/upstream/audit gates pass. Progressive backup failover, CDN ranking/cache, FLV, segmented `durl`, subtitles, danmaku, pages above 64 and broader Bilibili media remain unsupported or deferred. Every real login/API/CDN/Emby/Jellyfin row remains `NOT_RUN`.

Execution 0026 implementation `190488f`, planned in `0694934`, upgrades the bounded detail protocol to v6 and accepts one progressive `durl` primary plus at most eight validated ordered backups through equivalent `backup_url`/`backupUrl` aliases. Conflicting aliases, malformed values, duplicates, primary-equal candidates and overflow fail closed. A private single-page backup field and optional multipart `backup_urls` preserve historical primary-only compatibility, then disappear recursively before retained raw/SQLite/Job state. Ordinary progressive and DASH locators now share one primary-first candidate pass under the same asset lock, deadline, byte caps and restart budget. DNS, timeout, transport, interruption, HTTP and Range incompatibility may advance; network-policy, redirect/header/encoding, chunk/size, filesystem, probe, mux, archive and publication failures remain immediate. Cross-candidate resume requires exact offset, total length and validator type/value continuity; mixed failures retain valid partials, and destructive restart follows only a complete-pass rejection. One adapter all-`401`/`403` pass re-resolves detail once; a second all-auth pass returns `locator_refresh_auth_expired`, while direct and mixed/non-auth exhaustion do not refresh. Single-page and three-page SQLite compositions make every primary return `503`, reach ordered backups, pass controlled probe → SHA-256 archive → Emby primary/part publication and replay with zero new work. Signed candidates and private fields are absent from retained evidence. Focused `490 passed in 73.31s`; complete `1814 passed, 1 skipped in 342.33s`; both progressive compositions, DASH compatibility and all quality/build/docs/upstream/audit gates pass. Multiple `durl` segments, FLV, CDN ranking/racing/cross-run cache, mixed/non-auth exhaustion refresh, pages above 64 and broader Bilibili media remain unsupported or deferred. Every real login/API/CDN/real-progressive-byte/Emby/Jellyfin row remains `NOT_RUN`.

Execution 0027 implementation `7f99aa4`, planned in `ec7095a`, upgrades the bounded detail protocol to v7 and grants FLV authority only from a valid explicit top-level format. One repr-safe typed target crosses collision-safe single-/multipart private bridges and remains ephemeral. A generation-scoped source store reuses ordered candidates, strict resume/restart and one all-auth refresh; exact source-FLV probing precedes fixed bounded single-input `ffmpeg -c copy`, and only a final probing exactly as MP4 is archived/exported. Remux/final-gate failure retains the source but removes an unprepared final. A generated local H.264+AAC FLV traverses primary `503` → backup → production ffprobe/ffmpeg → dual-stream SHA-256 MP4 → Emby output and zero-work replay without signed/private/raw-FLV publication. Focused `394 passed in 59.12s`; complete `1848 passed, 1 skipped in 347.72s`; all quality/build/docs/upstream/audit gates pass. Multiple `durl` segments, FLV concatenation/transcoding and all live rows remain open or `NOT_RUN`.

Execution 0014 implementation `c4ab537` qualifies one Kuaishou ordinary single-video shape with exactly one valid play URL and an optional cover. The pinned process contract accepts a pure video ID through `KS_SPECIFIED_ID_LIST`; exact Account/Subscription-bound lazy refresh resolves both Assets with `MediaRequestProfile.DEFAULT`. Durable raw and SQLite keep only canonical origin/path and structurally remove userinfo, known/unknown query values, fragments and nested schema drift. Synthetic MP4/PNG bytes pass bounded default-profile HTTP without Cookie/Authorization, mandatory video probing, SHA-256 archive finalization and idempotent Emby `.mp4`/poster/NFO/source publication.

The Kuaishou creator crawl still traverses until upstream `no_more`, so this result does not prove bounded creator pagination. Galleries, multiple play URLs, audio/subtitles/comments, live/paid/restricted media, special CDN headers and same-ID/same-origin/path byte replacement remain unsupported or deferred; successful detail cleanup is proven, but injected cleanup failure still lacks full quarantine/incident/account blocking.

Execution 0015 implementation `95d314d` qualifies one narrower Douyin composition shape: a decimal `aweme_id`, empty `note_download_url` and `music_download_url`, exactly one video URL and an optional cover. The real isolated fake checkout exercises `MediaCrawlerDetailProcessRunner`, numeric `DY_SPECIFIED_ID_LIST`, media-off/detail JSONL configuration, saved profile and successful attempt cleanup. The platform composition test intentionally substitutes a fake detail runner, mock DNS/HTTP, synthetic MP4/PNG and a controlled probe while still traversing exact Account/Subscription/AssetRefreshSource selection, default-profile media HTTP, SHA-256 archive and Emby `.mp4`/poster/NFO/source publication. Default-profile media requests carry no Cookie, Authorization, Referer or Origin; this does not mean the separate Cookie-login detail contract contains no Cookie.

All four Douyin media raw fields now retain only canonical origin/path for accepted flat URLs. A scalar comma-delimited note list becomes an ordered sequence; mapping/nested values and comma-bearing sequence children fail closed per item, while comma-bearing non-note scalars fail closed as fields. This sanitizer regression covers gallery/audio-shaped inputs but does not qualify gallery playback, associated-music semantics or multiple media URLs. Query-only replay cannot detect byte replacement under the same aweme ID and origin/path, and a trusted Subscription is not independent proof of remote author ownership.

Execution 0016 implementation `a77ca74` installs an integration-owned, task-local Weibo image shim in creator and detail children only after the pinned checkout and imported store provenance have been verified; it never edits `.upstream`. It accepts only ordinary originals with a canonical positive numeric note ID, no `retweeted_status`, and no effective `page_info` (absent, `null` or empty object only). `pics` must be a non-empty flat ordered list of unique valid `pid`/URL pairs. Sources must be query-free HTTPS URLs on `sinaimg.cn` or its subdomains with case-insensitive static `.jpg`, `.jpeg`, `.png` or `.webp` filenames; the shim follows the pinned `https://i1.wp.com/<sina-host>/large/<filename>` transformation. A task-local `ContextVar` prevents concurrent note captures from crossing. One image becomes `ContentKind.IMAGE`; multiple images become `ContentKind.GALLERY`; ordered IMAGE Assets use positions `0..N-1`. The private capture field is removed recursively before retained raw metadata, while ingestion creates stable `adapter_refresh` identities and exact Account/Subscription-bound `AssetRefreshSource` observations.

Weibo detail authority is closed independently in the refresh context, parent `MediaCrawlerDetailRequest`, and child payload loader. Each accepts `detail_reference` only as `None` or an ordinary `str` exactly equal to the same canonical numeric content ID; a different ID, URL, `SecretValue`, string subclass or malformed value fails at that boundary. Real isolated fake-checkout contracts exercise the creator and detail process runners. A separate composition test intentionally uses a fake detail payload, mock public DNS/HTTP and two synthetic PNGs while traversing production provenance selection, `MediaRequestProfile.DEFAULT`, image validation, two immutable SHA-256 archive blobs and idempotent Emby/Jellyfin poster/backdrop/two-file-gallery/NFO/allowlisted-source layout. Default-profile media requests add no Cookie, Authorization, Referer or Origin. This is offline protocol evidence: all real login, creator scan, detail/proxy/CDN byte and Emby/Jellyfin server rows remain `NOT_RUN`.

Before a network-bearing pipeline creates or mutates any child Job/Asset lifecycle state, production preflight validates the pinned MediaCrawler lock, checkout and Python runtime and verifies that mandatory `ffprobe` is actually launchable. Bilibili DASH and explicit FLV-derivative paths also require launchable `ffmpeg` before durable child work. Missing and invalid-but-present configurations both fail before child lifecycle side effects. This is offline configuration coverage, not live CDN qualification.

The offline-supported refresh shapes are XHS image/video, Douyin image/video/audio/cover, Kuaishou ordinary single video plus optional cover, Bilibili cover plus compatible single-page or 2–64-page progressive/DASH ordinary uploads including explicit single-segment FLV-to-MP4 derivation, ordinary-original numeric-ID Weibo static IMAGE/GALLERY, one ordinary Zhihu answer with exactly one static IMAGE, and compatible one-/two-/3–64-image ordinary Tieba first floors. Execution 0027 passes all offline closeout gates in implementation `7f99aa4`; its FLV derivative keeps the twelve-shape count unchanged. Tieba galleries above 64 and mixed/rich/reply content, Zhihu multiple images/articles/zvideo, Weibo video/GIF/long-image handling, XHS broader media/expiry recovery, Douyin/Kuaishou expanded media and Bilibili multiple-segment/FLV-concatenation/transcoding shapes remain unsupported or deferred. Query-free `direct` locators continue to use the platform-independent downloader without enabling MediaCrawler.

Execution 0010 composes the durable local workflow without broadening platform support. Scheduler success only enqueues one `pipeline.subscription` coordinator. A separate explicit bounded `pipeline run` scans at most `--scan-limit` candidates per claim, renews the coordinator lease, downloads the exact Subscription's eligible assets sequentially and calls Emby export only after a durable recheck. MediaCrawler refresh stays default-off and license-gated in this second worker too. This is a one-shot control surface, not a daemon.

Composite API/access-key mapping names are redacted across snake_case, kebab-case, camelCase and provider-prefixed forms without erasing ordinary `key`, `public_key` or `key_id` fields. Credential-marker URL paths, including encoded and double-encoded variants, are redacted at sinks and rejected by both `direct` locators and source-hint derivation. Current ingestion and the `0003` legacy backfill therefore persist only a stable `adapter_refresh` identity for such an asset; the legacy unsafe `source_url` is cleared. On downgrade, `0003` also clears all asset download FKs and generation-bound Jobs, removes non-recoverable non-succeeded Emby identities, and preserves the succeeded publication chain plus structurally valid publication-intent recovery state.

## Storage and scheduling

| Capability | Upstream state | media-sync response |
| --- | --- | --- |
| Subscription table | Absent | Independent normalized schema |
| Run history | One in-memory WebUI process | Durable `sync_runs` and events |
| Incremental cursor | Absent | Known-ID + publish watermark + optional cursor |
| Idempotent upsert | SQL path does select-then-write | Database unique constraints and atomic upsert |
| JSONL isolation | Per-day append | Unique output root per run |
| Multi-account profile | Per platform only | Per platform and account |
| Interactive authentication | Platform login code may exit or fall back to QR implicitly | Execution 0011 adds an explicit double-gated QR command, closed child truth, durable LoginSession state and atomic `saved_session` handoff. Focused and complete offline gates pass; live qualification remains open |
| Durable scheduling | In-memory WebUI queue only | Execution 0006 provides durable due cycles, retry policy and platform/account launch lanes; execution 0007 adds the default-off, license-gated MediaCrawler forward handler with attempt roots, parent heartbeat/supervision and exact ingestion fencing. Its historical AC6/AC13 records remain `PARTIAL`; execution 0008 now passes their successor offline closeout with both remaining cancellation barriers and the exact 33-cell failure/sink matrix |
| Automatic downstream handoff | Absent | Execution 0010 atomically enqueues `pipeline.subscription` on sync success; an explicit bounded `pipeline run` performs sequential download then Emby export. No resident daemon or HA supervisor is implemented |

## Qualification status

Execution 0007 supplies automated offline evidence for all seven identifiers: subscribe → tick → manifest-v3 write/load → a real local fake child writes versioned JSONL → receipt-v2 write/read → guarded ingestion → retry/restart → idempotent replay. This proves the media-sync/child filesystem protocol and durable identities only. It does not use a browser, platform account, creator endpoint, CDN or media server, and it does not prove bounded upstream pagination or live compatibility.

No live account or interactive challenge has been used. All seven live QR/Cookie/saved-session login, creator traffic and scheduled-run entries remain `NOT_RUN`; phone login remains unsupported rather than merely untested. No live signed-locator refresh/CDN retrieval or authorized real-server connection probe, Library discovery, targeted-refresh acceptance, exact item lookup or post-refresh item observation has run. Execution 0007's own AC6/AC13 records remain historical `PARTIAL` evidence.

### Execution 0054 phase-B media-server truth

Qualification schema v2 exposes three independent dimensions instead of deriving qualification from local rows. `automated_evidence` contains bounded database counts and the latest allowlisted probe/scan Operation facts; it never confers a human PASS. `connection_probe`, `library_discovery`, `targeted_scan_acceptance`, `item_lookup` and `post_refresh_item_observation` have implementation status `IMPLEMENTED` and human status `NOT_RUN` in this workspace. `provider_task_completion` is `NOT_IMPLEMENTED` with reason `provider_api_unsupported`; `playback_evidence` and `automatic_post_export_scan` are also `NOT_IMPLEMENTED`. Those unimplemented capabilities have null human status.

Legacy `{}` scan success proves only targeted-refresh acceptance. Author observation proves only an absent baseline followed by one accepted refresh and two separated observations of the same unique provider/path item; it is postcondition evidence, not provider task completion or playback. The phase-B local gate passes 69 Web tests across 7 files plus format, Svelte check and production build, 11 real-PostgreSQL Operation race tests, and a complete Python suite of 2763 passed with 3 skips and 1 existing warning. The PostgreSQL fixture creates only the four production Operation/Event/Subject/StreamState metadata tables in an isolated schema; it is not evidence that the complete application schema or deployment supports PostgreSQL. This local/mock evidence uses no real Emby/Jellyfin credential and grants no human PASS. Execution 0055's backend single-operator access-control boundary is implemented in commit `f19bfaa`; its 190-test auth/API focus and complete offline regression (`2811 passed, 14 skipped, 1 warning`) pass, with the 11 real-PostgreSQL tests skipped because this workstation has no test URL. It provides fail-before-bind typed credentials, exact Host/Origin and deny-by-default route protection, rotating HttpOnly sessions with CSRF, and an optional distinct Bearer credential.

Execution 0055 adds a matched-only observation fingerprint to the read-only lookup response, synchronizes that response type in Web, advances Alembic to `0008_playback_evidence`, and adds the append-only model plus exact create-or-replay repository. The current checkpoint now implements and offline-verifies the browser-only confirmation backend: a bounded resolve A → complete unique lookup → resolve B authority check finishes before the short database transaction, and the POST requires exact Cookie/Origin/CSRF while rejecting Bearer-only, mixed Authorization, `Idempotency-Key` and every request field except canonical author UUID plus opaque fingerprint. Its response contains only the safe evidence/author IDs, server timestamps and replay state. The complete current tree passes `2941 passed, 22 skipped, 1 warning in 594.72s`; Web 69 plus format/check/build and all locally available static/package/docs gates pass. Real PostgreSQL and Docker remain `NOT_RUN`, and the isolated Author/Job/PlaybackEvidence harness is not complete-schema or deployment evidence. Console v2 and `/legacy` still lack login/session/CSRF and confirmation UI, while the bounded by-author current/stale projection and qualification schema v3 do not exist. Qualification therefore stays at schema v2, where the overall `playback_evidence` capability remains `NOT_IMPLEMENTED` with null human status; every live row remains `NOT_RUN`. Browser-writable settings, multiple profiles, retention/destructive maintenance and automatic post-export scanning remain unimplemented; the latter has no frozen follow-up assignment.

Execution 0011 local commit `8bb16f6` established the historical offline state and normal-process boundaries for explicit QR login, expired saved-session reauthentication and non-interactive saved-session reuse. Execution 0012 commit `28655f8` now adds bounded request/result framing, continuing parent control, a post-result guardian, hard-parent-death complete-tree containment and deadline-fenced abandoned-session recovery under the same account lock. Login start, redaction-safe status and the resident sweep make recovery reachable; a rotating cursor prevents busy early candidates from starving later accounts. The 0012 focused gate passes 283 tests with one Windows-inapplicable skip and the full suite passes 1156 with the same skip. A false upstream saved-session probe may still include network ambiguity, so `auth_expired` remains conservative rather than an exact remote-cause diagnosis. This evidence changes no live qualification row.

Execution 0009's refresh MVP, execution 0010's explicit bounded downstream pipeline, execution 0012's local foreground supervisor and the twelve frozen media shapes established through execution 0024 are complete for their frozen offline scopes; executions 0025–0026 add ordered backup reliability and execution 0027 adds explicit single-segment FLV-to-MP4 derivation. Execution 0027's focused regression passes 394 tests, its complete suite passes 1848 with one Windows-inapplicable skip, the Bilibili compatibility/production compositions and all quality/audit gates pass. At that historical boundary, evidence covered twelve frozen media shapes across all seven platforms but no live platform/CDN/media-server row. The production pipeline handler was synchronous and ran through `asyncio.to_thread`; the supervisor therefore drained one already-active attempt under heartbeat instead of cancelling it. Forced underlying-thread termination and multi-worker HA remained unqualified.

At that same historical boundary, Weibo video/effective `page_info`, GIF/animated semantics, long-image-specific handling, bounded creator pagination, a direct-Sina request profile, same-ID media replacement detection and injected cleanup-failure quarantine were unavailable or deferred. The larger product also lacked Tieba galleries above 64 images, mixed/rich/reply media and replacement semantics, Zhihu multiple-image/article/zvideo media, complete seven-platform media-shape coverage, XHS broader media/authority-expiry recovery, Bilibili multiple-segment progressive media and FLV concatenation/transcoding, pages above 64, subtitles/danmaku, CDN ranking/racing/cross-run cache and fresh-detail refresh after mixed/non-auth exhaustion, derivatives on other platforms, per-request HTTP spacing, and forced synchronous-thread cancellation/multi-worker HA. Those were implementation gaps, not `NOT_RUN` outcomes; the live qualification rows at that boundary remained `NOT_RUN`. The shipped supervisor was an explicit single-host foreground process, not a production service.
