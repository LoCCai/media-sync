**English** | [中文](roadmap.zh.md)

# Delivery roadmap

## Product outcome

Build a self-hosted service that can authenticate against all platforms supported by the pinned MediaCrawler release, subscribe to authors, incrementally collect their posts, images and videos, and export an Emby/Jellyfin-compatible library.

## Phases

### Phase 0 — Baseline and legal boundary

- Pin both upstream repositories and capture their licenses.
- Inventory platform/login/creator/media capabilities.
- Establish the execution journal and bilingual Git conventions.
- Acceptance: sources are reproducible by SHA; the license boundary is explicit; the roadmap is reviewable.

### Phase 1 — Core domain and persistence

- Python package, configuration and CLI skeleton.
- SQLite schema for accounts, credential references, authors, subscriptions, content, assets and sync runs.
- Adapter protocol plus a deterministic Fake adapter for tests.
- Acceptance: migrations initialize a database; CRUD and state transitions pass automated tests.

### Phase 2 — MediaCrawler bridge

- Status: offline command construction, fixture normalization and sealed ingestion were delivered; the authorized live smoke test remains `NOT_RUN` because no account or interactive challenge was authorized.

- Discover and validate a pinned external MediaCrawler checkout.
- Translate login mode and creator subscription into isolated crawler jobs.
- Ingest JSON/JSONL results into the normalized domain without importing or copying restricted source.
- Acceptance: dry-run command construction and fixture ingestion work for all seven platform identifiers; one authorized live smoke test is documented separately.

### Phase 3 — Download and Emby export

- Status: the execution 0005 platform-neutral offline foundation is complete. Executions 0013–0024 establish twelve frozen media shapes across all seven platforms; executions 0025–0026 add ordered DASH/progressive backup reliability, execution 0027 adds explicit exactly-one-segment FLV-to-MP4 derivation, execution 0029 adds bounded 2–64 ordinary multi-segment `durl` per-segment download with one-shot concat, execution 0030 extends that concat to multi-segment FLV, and execution 0031 adds the first playable Weibo video shape, execution 0032 adds the bounded Douyin note gallery, execution 0033 adds the bounded Zhihu answer gallery, execution 0034 adds the bounded Kuaishou atlas gallery, execution 0035 adds Weibo `playback_list` quality selection, execution 0036 adds the Weibo video poster, execution 0037 adds the bounded XHS multi-video tuple, and execution 0038 adds the XHS live photo — none changing stable identity. Exact canonical detail refresh, closed request profiles, strict cross-candidate resume/restart, source/final probing, bounded ffmpeg stream-copy, SHA-256 archive and deterministic Emby publication pass for their frozen slices. The current focused regression passes 355 tests, the complete suite passes 2032 with one Windows-inapplicable skip, and all quality/build/docs/upstream/audit gates pass. `playback_list`/quality selection, posters, retweets, GIF/live/paid media, mixed media posts, multi-image live galleries, broader Tieba media, transcoding/codec repair, pages above 64, subtitles/danmaku, CDN ranking/racing/cross-run cache and mixed/non-auth exhaustion refresh; and other documented expanded shapes remain open. Real login/creator/detail/play/CDN/platform-byte and Emby/Jellyfin server validation remain `NOT_RUN`.

- Download assets with resumability, checksums, type/size limits and safe filenames.
- Map video and image posts into stable creator/content directories.
- Generate XML NFO plus cover, backdrop and sidecar manifest files.
- Acceptance: fixture content renders a library that passes XML validation and golden-tree tests.

### Phase 4 — Scheduler, API and operations

- Status: execution 0009's function-first refresh MVP is implemented in commit `98cf387`, execution 0010's durable enqueue plus explicit bounded pipeline worker is complete in commit `f2e5899`, execution 0011's explicit QR-login/saved-session handoff is complete in commit `8bb16f6`, and execution 0012's hard-parent-death containment, deadline-fenced login recovery and local foreground supervisor are complete in commit `28655f8`. The 0012 focused gate passes 283 tests with one skip; the full suite passes 1156 with the same Windows-inapplicable skip. The foreground supervisor is not an installed/auto-restarting daemon. Cross-host HA, seven-platform complete download and live platform/CDN/Emby qualification remain unclaimed; every live row remains `NOT_RUN`.

- Job scheduler, retry/backoff and launch concurrency/throttling: delivered for the execution 0006 Fake/offline boundary.
- Local REST API for account, subscription, sync and export operations: planned; the equivalent CLI control surfaces are partially implemented.
- Health/readiness endpoints, structured logs and Docker packaging: planned/deferred.
- Execution 0007 offline acceptance: policy/artifact/attempt identity, off-loop parent heartbeat, parent-death/control handshake, exact fencing, conservative status mapping, four-state cleanup and the seven-platform protocol chain are implemented and exercised. Repeated runner cancellation and a deterministic between-batch cancellation barrier now prove join-before-unwind and no second-batch mutation. AC6 remains `PARTIAL` only because deterministic child-exit/pre-seal and post-seal/pre-ingest barriers are incomplete. AC13 is `PARTIAL`: cleanup/redaction/sentinel coverage is substantial, but the full known-secret/nonzero/timeout/all-limits/receipt/cancel/lease-loss × retained-filesystem/SQLite/operator-sink matrix is incomplete.
- Execution 0008 completes the offline successor closeout for those two partial criteria: the child-exit/pre-seal and single/repeated post-seal/pre-ingest barriers pass; the exact eleven-failure × three-sink matrix proves 33 cells with fail-closed filesystem/SQLite scanning and fixed operator authority. The full suite passes 837 tests with one Windows-inapplicable skip and 79% branch-aware coverage. Refresh and DAG work remain excluded from this result.
- Execution 0009 delivery: exact current provenance, bounded detail refresh and explicit default-off asset-download wiring are implemented. At that historical boundary, offline shapes were XHS image/video, Douyin image/video/audio/cover, Kuaishou video/cover and Bilibili cover; execution 0013 extends the current Bilibili capability below. XHS multi-note authority lookup and additional platform assets remain later work.
- Execution 0010 delivery: sync success atomically enqueues one `pipeline.subscription` coordinator and stops. A separate operator-invoked `pipeline run` command scans a bounded queue, renews its lease, downloads exact-subscription assets sequentially and then calls the existing Emby publisher. Existing Job schema, generation recovery and publication recovery are reused without a migration.
- Execution 0012 delivery: login hard-parent-death containment, deadline-fenced/fair LoginSession recovery and a local foreground supervisor now cover scheduler, subscription and pipeline phases. Shutdown cancels and joins MediaCrawler sync, while one already-active thread-backed pipeline attempt remains heartbeat-protected and is drained exactly—even under repeated task cancellation—instead of being falsely called force-stopped. The next product slices are additional playable platform media, local REST/operations and authorized per-platform/CDN/real-media-server qualification; each must retain explicit unsupported versus `NOT_RUN` distinctions.
- Execution 0013 delivery (`dd6cfec`): one Bilibili ordinary numeric-aid upload now contributes a stable `NULL`-source `<aid>:video:0` Asset. Exact Subscription-bound refresh validates the current first CID, accepts exactly one progressive `durl`, carries a closed UA/Referer/Origin request profile without Cookie/Authorization, and composes synthetic bytes through controlled probing, archive finalization and idempotent Emby `.mp4` publication. The focused gate passes 223 tests and the complete suite passes 1199 with one Windows-inapplicable skip. Because forward metadata lacks CID, a same-aid first-CID replacement cannot automatically invalidate already-verified bytes. DASH/mux, FLV remux, multiple segments/pages, subtitles, danmaku, backup failover and paid/bangumi/live media remain deferred; real account/CDN/media-server rows remain `NOT_RUN`.
- Execution 0014 delivery (`c4ab537`): one Kuaishou ordinary record with exactly one valid play URL and an optional cover now has locked raw-ID detail/process evidence, exact Account/Subscription-bound lazy refresh and deterministic MP4/PNG download through the default request profile. Durable raw removes userinfo/query/fragment and nested schema drift; video probing, SHA-256 archive and idempotent Emby `.mp4`/poster/NFO/source publication pass. The focused gate passes 228 tests and the complete suite passes 1206 with one Windows-inapplicable skip. Same-ID/same-path byte replacement, bounded creator pagination, galleries/multiple URLs, special CDN headers, cleanup-failure quarantine and all live rows remain deferred or `NOT_RUN`.
- Execution 0015 delivery (`95d314d`): one ordinary numeric-ID Douyin record with empty note/music fields, exactly one video URL and an optional cover now has a pinned pure-ID detail/process contract, exact Account/Subscription-bound lazy refresh and deterministic MP4/PNG download through the default media profile. Durable raw removes userinfo/query/fragment across all four media fields, turns scalar note comma lists into ordered items and rejects nested/comma schema drift per item. Controlled video probing, SHA-256 archive and idempotent Emby `.mp4`/poster/NFO/source publication pass; query-only replay adds no detail, HTTP, DNS or probe call. The focused gate passes 231 tests and the complete suite passes 1209 with one Windows-inapplicable skip. Galleries, associated-music semantics, multiple media URLs, same-ID/same-path byte replacement, bounded creator pagination, special CDN headers and every live row remain deferred or `NOT_RUN`.

- Execution 0016 delivery (`a77ca74`): one frozen ordinary-original Weibo static-image shape now captures ordered, unique `mblog.pics` in both creator and numeric-ID detail children without modifying `.upstream`, normalizes one/many accepted query-free `.jpg/.jpeg/.png/.webp` `i1.wp.com` locators embedding a `sinaimg.cn` source host into IMAGE/GALLERY Assets, and composes exact Account/Subscription refresh, mock transfer, SHA-256 archive and idempotent Emby poster/backdrop/gallery/NFO/source output. The focused gate passes 388 tests in 125.73 seconds; the full suite passes 1251 with one Windows-inapplicable skip in 359.38 seconds. Weibo video, retweets, media `page_info`, GIF/animation, bounded creator pagination, direct-Sina CDN behavior and all live login/creator/detail/CDN/platform-byte/Emby-Jellyfin-server rows remain excluded, deferred or `NOT_RUN`; other unqualified seven-platform media shapes remain open.

- Execution 0017 delivery (plan `9d19e7e`, implementation `2f8dbaa`): at this historical boundary, XHS lazy refresh keeps the explicit ephemeral exact-note reference as an override and otherwise follows the exact `AssetRefreshSource → Subscription` to privately resolve `policy.mediacrawler.creator_input.secret_ref`. The creator URL and author are validated at the refresh-context, parent-request and child-loader boundaries; creator/detail authority is mutually exclusive, the creator run is capped by `Subscription.max_items`, and multiple returned JSONL records are filtered to the exact content ID and Asset kind/position/source hint. Ordinary `type="normal"` one/many static images compose through default-profile mock HTTP, immutable SHA-256 archive and idempotent Emby poster/backdrop/gallery/NFO/source publication without retaining signed authority or query values. The complete suite passes 1298 tests with one Windows-inapplicable skip. At that boundary, automatic XHS video, live photo, animation, mixed media and authority-expiry recovery remained deferred; Tieba/Zhihu downloadable-Asset discovery remained unimplemented, and every real login/creator/detail/CDN/platform-byte/Emby-Jellyfin-server row remained `NOT_RUN`.

- Execution 0018 delivery (plan `c9d3586`, implementation `356e254`): automatic XHS creator fallback now accepts one ordinary raw `type="video"` row only when scalar `video_url` has exactly one candidate and scalar `image_list` has zero or one candidate, mapped one-to-one to one position-zero VIDEO plus optional IMAGE. The initial URL is restricted to a valid HTTP/HTTPS `xhscdn.com` host, default port and non-root path; malformed, foreign, userinfo, fragment, multiple and container-drift values fail closed, while redirects keep the existing public-network policy. A locked-source contract, real fake checkout, embedded real H.264 production-ffprobe check and deterministic SQLite → mock DNS/HTTP → SHA-256 archive → Emby `.mp4`/poster/NFO/source composition pass; query-only replay adds no work. The focused gate passes 222 tests and the complete suite passes 1353 with one Windows-inapplicable skip. Multi-video, multi-image, broader mixed/live-photo/animation, authority-expiry recovery and Tieba/Zhihu media shims remain future work; every real login/creator/detail/CDN/platform-byte/Emby-Jellyfin-server row remains `NOT_RUN`.

- Execution 0019 delivery (plan `dc1714c`, implementation `2edb9d7`): a verified-checkout shim captures exactly one static-image candidate from an ordinary Zhihu creator answer at the pinned extractor/store loss boundary. It uses frozen `data-original` → `data-actualsrc` → `src` priority; competing or duplicate selected attributes, `srcset`/`data-src`/lazy candidates, multiple images, playable/container drift and invalid canonical URLs fail closed. Capture is bound to the exact returned model, with `ContextVar` used only inside nested storage, so gather-child extraction reaches parent-task storage without peer leakage or private-field serialization. The scheduled creator answer loop is bounded by Subscription `max_items`, so Zhihu is removed from the full-history-acknowledgement set. ARTICLE plus one `<content_id>:image:0` IMAGE traverses exact persisted canonical-answer refresh, credential-free DEFAULT-profile mock HTTP, bounded JPEG/PNG/WebP structural qualification, SHA-256 archive and Emby poster/backdrop/gallery/body/NFO/source output with zero-work query replay; GIF/APNG/animated WebP/AVIF fail the structural gate. The final focused gate passes 505 tests in 48.82 seconds, the complete suite passes 1543 with one Windows-inapplicable skip in 318.39 seconds, all quality/audit gates pass, and an independent 461-test review finds no P0/P1/P2. No real redacted fixture exists; all live rows remain `NOT_RUN`; multiple images, articles, zvideo and Tieba media remain open.

- Execution 0020 delivery (plan `df7a38a`, implementation `8a0e935`): a verified-checkout shim captures exactly one current integer type-3 static image from an ordinary Tieba thread first floor before the pinned extractor discards locators. Exact-object binding crosses gather-child → parent-store and uses `ContextVar` only for nested storage. The scheduled creator wrapper validates page shapes and makes `max_items=23` exactly `20 + 3` successful detail/callback rows with no third page or post-cap sleep. ARTICLE plus one `<note_id>:image:0` IMAGE persists only a query-free hint; canonical SQLite thread authority drives a numeric-ID detail run and credential-free DEFAULT-profile signed refresh. Qualified JPEG/PNG/WebP pass the structural gate; GIF/APNG/animated WebP/AVIF fail. Deterministic SQLite → fake detail → mock DNS/HTTP → SHA-256 archive → Emby poster/backdrop/gallery/body/NFO/source composition and query-only zero-work replay pass. The focused regression passes 368 tests and the complete suite passes 1650 with one Windows-inapplicable skip; all quality/build/upstream/audit gates pass. Real login/creator/detail/CDN and Emby/Jellyfin server rows remain `NOT_RUN`; galleries, other first-floor content types and broader Tieba media remain open.

- Execution 0021 delivery (plan `5095ed6`, implementation `e0fb8d5`): the v1 single-image Tieba field remains compatible while a separate v2 field captures exactly two ordered, distinct type-3 image identities across the same pinned gather-child → parent-store boundary. ARTICLE now emits `<note_id>:image:0/1`; both private fields are recursively stripped and SQLite retains only the ordered query-free hints. Lazy refresh binds the complete persisted gallery and resolves either position only when the current detail reproduces both identities in order; missing, reordered, replaced, duplicated or dual-claimed results fail closed. Two DEFAULT-profile downloads pass JPEG/PNG static qualification, publish two SHA-256 archives and compose Emby poster/backdrop/two gallery files/body/NFO/source; query-only replay adds zero work. Focused 413 and full 1668+1-skip gates plus all quality/build/docs/upstream/audits pass. At that historical boundary, three-or-more images remained open; mixed/rich/reply media, replacement semantics and all live rows remain open or `NOT_RUN`.

- Execution 0022 delivery (plan `fbcb7cf`, implementation `b6d03aa`): v1 remains exactly one image and v2 exactly two while a separate v3 claim captures 3–64 ordered, distinct type-3 images across the same exact-object gather-child → parent-store boundary. ARTICLE emits `<note_id>:image:0..N-1`; all three private fields are recursively stripped and SQLite retains only the complete ordered query-free hint tuple. The shared 64-image cap is enforced by capture, normalization, refresh context and lazy database loading; 65 images, malformed/duplicate items and multi-version claims fail closed. Every refresh position requires complete current size/order/identity/position/remote-ID equality, rejecting missing, added, reordered, replaced and duplicated galleries. Three DEFAULT-profile JPEG/PNG/WebP downloads publish three SHA-256 archives and compose Emby poster/backdrop/three gallery files/body/NFO/source; query-only replay adds zero work. Focused `433 passed in 48.91s`, full `1688 passed, 1 skipped in 321.22s` and all quality/build/docs/upstream/audits pass. Galleries above 64 images, mixed/rich/reply media, replacement semantics and all live rows remain open or `NOT_RUN`.

- Execution 0023 delivery (plan `bd45478`, implementation `24fd41c`): a source-verified, task-local shim captures 1–64 canonical ordered `page`/`cid` identities before the pinned Bilibili JSONL store drops `View.pages`; 65, malformed, non-contiguous and duplicate-CID declarations fail closed. Exact single-page `<aid>:video:0` remains compatible, while qualifying 2–64-page uploads emit ordered locator-only `<aid>:video:cid:<cid>` VIDEO Assets. Detail protocol v4 receives only the target `bili_video_cid`, calls that CID's play API and accepts exactly one progressive `durl`; lazy refresh binds the complete persisted sibling tuple and rejects missing, added, reordered, replaced, duplicated or malformed current pages. Private fields and signed URLs are recursively stripped before persistence. Three targeted Bilibili-profile downloads publish distinct SHA-256 archives and deterministic Emby primary/two-part/NFO/source output; query-only replay adds zero detail/DNS/HTTP/probe/archive/export work. Focused `436 passed in 53.96s`, full `1739 passed, 1 skipped in 321.25s` and all quality/build/docs/upstream/audits pass. DASH/mux, multiple `durl` segments, FLV, subtitles/danmaku, backup failover, pages above 64, broader Bilibili types and all live rows remain open or `NOT_RUN`.

- Execution 0024 delivery (plan `a7d038e`, implementation `12314b9`): strict Bilibili detail protocol v5 requests the exact CID with WBI signing, `qn=127`, `fourk=1` and `fnval=4048`; selection chooses the highest supported video quality, AVC → HEV → AV1 at equal quality, the pinned ordinary/Dolby/Hi-Res audio order and a valid silent shape. Signed primary/backup/component URLs remain runtime-only. Generation-scoped stores provide strict resume, component probes, a combined byte cap, fixed bounded `ffmpeg -c copy`, final `ffprobe` and final-only immutable publication. Mux failure retains verified components; a prepared published final recovers without detail/DNS/HTTP/ffmpeg. A real local H.264+AAC composition produces a final archive and Emby MP4 with both streams. Focused `456 passed in 66.47s`, full `1780 passed, 1 skipped in 333.43s`, production ffmpeg/ffprobe integration and all quality/build/docs/upstream/audits pass. Backup URLs are modeled but failover remains open; every live row remains `NOT_RUN`.

- Execution 0025 delivery (plan `8e9467d`, implementation `fe45abc`): each selected ephemeral DASH video and optional audio component now tries its primary then at most eight validated distinct backups in source order under the existing asset lock, byte caps and shared deadline. DNS, timeout, transport, interruption, HTTP and partial-Range incompatibility may advance; network-policy, redirect/header/encoding, chunk/size, filesystem, probe and mux failures remain immediate. Cross-candidate append requires exact offset, total length and validator continuity; mixed failures preserve partial bytes, while full-pass rejection is required before bounded discard/restart. All-`401`/`403` exhaustion remains `locator_refresh_auth_expired`, and URLs/hosts/winning indices are not retained. A production-process video-`503` plus audio-`403` composition reaches independent backups and publishes a dual-stream archive/Emby MP4. Focused `466 passed in 66.96s`, full `1790 passed, 1 skipped in 331.33s`, real ffmpeg/ffprobe backup-path integration and all quality/build/docs/upstream/audits pass. Progressive backup failover, CDN ranking/cache and every live row remain open or `NOT_RUN`.

- Execution 0026 delivery (plan `0694934`, implementation `190488f`): strict detail protocol v6 accepts exactly one progressive `durl` primary plus at most eight validated ordered backups through equivalent `backup_url`/`backupUrl` aliases. Bounded private single-/multipart bridges preserve historical primary-only compatibility and are recursively stripped before persistence. Ordinary progressive and DASH locators share one primary-first candidate pass; eligible failure classes, exact cross-candidate offset/length/validator continuity, partial retention and whole-pass restart remain closed. Only an all-`401`/`403` adapter pass refreshes detail once; a second all-auth pass returns `locator_refresh_auth_expired`, while direct and mixed/non-auth exhaustion do not refresh. Single-page and three-page compositions make each primary return `503`, reach its backup, publish archive/Emby output and replay with zero new work without retaining signed candidates. Focused `490 passed in 73.31s`, full `1814 passed, 1 skipped in 342.33s`, progressive backup compositions, DASH compatibility and all quality/build/docs/upstream/audits pass. Multiple `durl` segments, FLV, CDN ranking/racing/cross-run cache, mixed/non-auth exhaustion refresh and every live row remain open or `NOT_RUN`.

- Execution 0027 delivery (plan `ec7095a`, implementation `7f99aa4`): strict detail protocol v7 grants FLV authority only from a valid explicit top-level format and reconstructs one repr-safe typed target through collision-safe single-/multipart private bridges. A generation-scoped source store reuses ordered candidates, strict Range/validator continuity, whole-pass restart and one all-auth refresh. Exact FLV source probing precedes fixed bounded single-input `ffmpeg -c copy`; only a final probing exactly as MP4 publishes, while failures retain the source and remove unprepared finals. A generated local H.264+AAC FLV reaches production ffprobe/ffmpeg, dual-stream SHA-256 MP4 and Emby output after primary `503`/backup failover, then replays with zero work and retains no signed/private/raw-FLV publication. Focused `394 passed in 59.12s`, full `1848 passed, 1 skipped in 347.72s` and all quality/build/docs/upstream/audits pass. Multiple `durl` segments, FLV concatenation/transcoding and every live row remain open or `NOT_RUN`.
- Execution 0029 delivery (plan `9a40968`, implementation `7eb188d`): strict detail protocol v8 accepts a bounded 2–64 ordered ordinary (non-FLV) multi-segment `durl` tuple, each segment with one primary plus at most eight backups, while exactly-one-segment payloads stay byte-compatible and top-level FLV with multiple segments stays unsupported. One repr-safe `ResolvedSegmentsLocator` crosses a collision-checked `{"cid", "segments"}` private bridge for single-page and multipart page tuples, strips before persistence and reconstructs only on an exact payload-CID match. The typed downloader branch downloads segments in order under one shared byte cap/deadline with the established candidate failover, probes each segment exactly as MP4, allows one all-auth refresh that must return the same segment count, and concatenates through one fixed concat-demuxer `ffmpeg -c copy` invocation whose relative-name script lives only inside the confined parts directory; only an exactly-MP4 final publishes, segments stay resumable on failure, prepared finals recover without network and cleanup discards every segment store. A real two-segment H.264+AAC composition traverses SQLite → failed primary → backup → production ffprobe/ffmpeg → SHA-256 MP4 → Emby with zero-work replay and no retained signed URL or private marker. Focused `447 passed in 70.97s`, full `1902 passed, 1 skipped in 409.85s` and all quality/build/docs/upstream/audits pass; two test-only `tasklist` decoding helpers were repaired for the Chinese-locale Windows workstation (a pre-existing clean-tree failure). Multi-segment FLV concatenation, CDN ranking/racing/cross-run cache and every live row remain open or `NOT_RUN`.
- Execution 0030 delivery (plan `e7395fb`, implementation `564f80f`): strict detail protocol v9 grants multi-segment FLV authority only from the closed top-level format classification plus the bounded 2–64 `durl` tuple. A repr-safe `ResolvedFlvSegmentsLocator` wraps exactly one `ResolvedSegmentsLocator` and crosses the existing private bridge with an exact `"format": "flv"` marker; non-exact markers, flat-field collisions and page-less payloads fail closed, stripping recurses before persistence, and exactly-one-segment FLV, multi-segment ordinary and DASH payloads stay byte-compatible. The typed downloader branch accepts both segment target types with flavor-bound resume fingerprints, per-segment exactly-FLV structural probing, one all-auth refresh that must return the same typed target and segment count, and one fixed concat-demuxer `ffmpeg -c copy` invocation; only an exactly-MP4 final publishes, failures retain resumable segments, and no raw FLV is archived, exported or published. A real two-segment H.264+AAC FLV composition traverses SQLite → failed primary → backup → production ffprobe/ffmpeg → SHA-256 MP4 → Emby with zero-work replay. Focused `460 passed in 91.95s`, full `1916 passed, 1 skipped in 446.64s` and all quality/build/docs/upstream/audits pass. Transcoding, codec repair, CDN ranking/racing/cross-run cache and every live row remain open or `NOT_RUN`.
- Execution 0031 delivery (plan `1c79c6d`, implementation `666438d`): the pinned Weibo store shim captures exactly one scalar `media_info.stream_url` from an ordinary, original, numeric-ID `mblog` whose `page_info.page_type` is exactly `video`, across the same exact-object boundary as the 0016 image capture; retweets and non-video page types capture nothing. One private `{"url"}` payload crosses with strict collision checks, joins the recursive pre-persistence strip set and is bound to a closed signed-URL validator (HTTPS `sinaimg.cn`/`*.sinaimg.cn`/`f.video.weibocdn.com` host, non-root `.mp4` path, no fragment/userinfo/port). `_normalize_wb` materializes `ContentKind.VIDEO` with one `{note_id}:video:0` VIDEO asset and quarantines dual-field, retained-`page_info`, retweet and malformed drift; WB VIDEO joined the adapter-refresh support set so one numeric-note detail child re-captures the current signed URL in memory and returns a DEFAULT-profile ephemeral locator while durable state keeps only the query-free hint. A production SQLite → refresh → mock DNS/HTTP → MP4 probe → SHA-256 archive → Emby `.mp4`/NFO/source composition passes with zero-work replay and no retained signed URL, query or private field. Focused `302 passed in 4.04s`, detail contracts `100 passed in 70.92s`, full `1956 passed, 1 skipped in 408.57s` and all quality/build/docs/upstream/audits pass. `playback_list`/quality selection, posters, retweets, mixed media and every live row remain open or `NOT_RUN`.
- Execution 0032 delivery (plan `286dac9`, implementation `95758c2`): the pinned comma-joined Douyin `note_download_url` gallery is frozen into a strict all-or-nothing parser — every item must be exactly one valid URL without embedded commas, duplicates and malformed items quarantine as `INVALID_RECORD` instead of being silently dropped, and the gallery bound is 1–64 images. `_normalize_dy` materializes `ContentKind.IMAGE` (one) or `ContentKind.GALLERY` (2–64) with ordered `{aweme_id}:image:0..N-1` IMAGE assets while the video/music/cover fields keep their tolerant parsing and the empty-field fallbacks stay byte-compatible; one pre-existing integration fixture asserting silent item dropping was updated to the quarantine contract. Each gallery position re-resolves its current signed URL through one exact numeric-ID detail run and path drift closes as `locator_refresh_asset_mismatch`. A production SQLite → refresh → mock DNS/HTTP → static PNG sniff gate → SHA-256 archive → Emby poster/backdrop/two gallery images/NFO composition passes with zero-work replay and no retained signature, sentinel or signed URL. Focused `316 passed in 5.09s`, DB-ingestion contracts `25 passed in 2.64s`, full `1971 passed, 1 skipped in 390.84s` and all quality/build/docs/upstream/audits pass. Mixed video+image Asset semantics, gallery music, animated drift, same-ID byte replacement and every live row remain open or `NOT_RUN`.

- Execution 0033 delivery (plan `92651bc`, implementation `966ccef`): the Zhihu extractor-boundary shim materializes one complete ordered tuple for 2–64 static images with per-image attribute-priority selection and pairwise distinctness, while the exactly-one-image v1 capture stays byte-compatible and above-bound, invalid, duplicated or forbidden-media answers capture nothing. One private v2 field crosses with strict collision checks and recursive stripping; `_normalize_zhihu` materializes ARTICLE with `{content_id}:image:0..N-1` IMAGE assets and quarantines dual-field and malformed drift. The lazy refresh binds the complete persisted sibling tuple through an app-assembled `zhihu_image_source_hints` context so each position re-resolves its current signed URL through one exact canonical-answer detail child run, with drift closing as `locator_refresh_schema_changed`. A production SQLite → refresh → mock DNS/HTTP → static PNG gate → SHA-256 archive → Emby poster/backdrop/two gallery images/body/NFO composition passes with zero-work replay. Focused `538 passed in 71.18s`, full `1984 passed, 1 skipped in 336.62s` and all quality/build/docs/upstream/audits pass. Articles, zvideo, animated drift and every live row remain open or `NOT_RUN`.

- Execution 0034 delivery (plan `eeff45e`, implementation `26c2b3e`): a pinned-store shim captures exactly the frozen `photo.ext_params.atlas.pics[].cdn` shape (1–64 pairwise-distinct HTTPS static-extension candidates) at the `update_kuaishou_video` boundary in both children; insecure, duplicated or above-bound atlases capture nothing. `_normalize_ks` materializes IMAGE/GALLERY with ordered `{video_id}:image:N` assets plus the optional COVER companion while ordinary video photos stay byte-compatible. KS IMAGE joined the adapter-refresh support set so each position re-resolves its current signed URL through one exact numeric-ID detail child run. A production SQLite → refresh → mock DNS/HTTP → static gates → SHA-256 archive → Emby two-image gallery composition passes with zero-work replay. Focused `445 passed`, detail contracts `106 passed in 69.98s`, full `2002 passed, 1 skipped in 352.79s` and all quality/build/docs/upstream/audits pass. Atlas captions, animated drift, mixed semantics and every live row remain open or `NOT_RUN`.
- Execution 0035 delivery (plan `ecc08da`, implementation `f2f4bc9`): the Weibo video capture gains a bounded closed `playback_list` fallback selecting the highest valid entry under `1080p > 720p > 540p > 480p > 360p` with 0031-validator revalidation; the scalar path stays first and byte-compatible, and unusable shapes capture nothing. Real-child contracts prove selection and closure; integration proves scalar-equivalent normalize/download/archive/Emby with zero-work replay. Focused `451 passed`, full `2010 passed, 1 skipped in 360.55s` and all quality/build/docs/upstream/audits pass. Dolby/Hi-Res labels, posters and every live row remain open or `NOT_RUN`.
- Execution 0036 delivery (plan `1ad49a7`, implementation `72e9f62`): the Weibo shim captures the closed `page_info.pic_info.pic_big.url` poster only alongside a capturable video, a new private field crosses with strict collision checks, and `_normalize_wb` materializes the `{note_id}:cover:0` COVER asset while WB COVER joined the adapter-refresh support set. A production two-asset SQLite → refresh → download → archive → Emby poster composition passes with zero-work replay. Focused `341 passed in 4.29s`, full `2016 passed, 1 skipped in 370.47s` and all quality/build/docs/upstream/audits pass. Alternate poster sizes and every live row remain open or `NOT_RUN`.
- Execution 0037 delivery (plan `d858147`, implementation `c5682e5`): the comma-joined XHS `video_url` scalar freezes into a bounded 1–16 ordered multi-video shape with above-bound normalization quarantine, a widened refresh scalar and a creator-target binding over the complete video tuple. A two-video integration composition downloads both positions and publishes two Emby episodes with zero-work replay. Focused `344 passed in 6.32s`, full `2020 passed, 1 skipped in 370.56s` and all quality/build/docs/upstream/audits pass. Live photos and every live row remain open or `NOT_RUN`.
- Execution 0038 delivery (plan `650c256`, implementation `8c80073`): a new pinned-store shim captures the frozen `image_list[0].live_photo.stream.h264[0].master_url` for a one-image `type="normal"` note, `_normalize_xhs` materializes MIXED with one IMAGE plus one VIDEO and the creator fallback binds the exact shape. Shared fake fixtures gained the minimal store modules. A production two-asset composition publishes the Emby episode with poster, zero-work replay. Focused `355 passed in 6.92s`, detail contracts `116 passed in 80.18s`, full `2032 passed, 1 skipped in 371.84s` and all quality/build/docs/upstream/audits pass. Multi-image live galleries and every live row remain open or `NOT_RUN`.
### Phase 5 — Platform qualification

- Qualify XHS, Douyin, Kuaishou, Bilibili, Weibo, Tieba and Zhihu using user-authorized accounts.
- Document login steps, expected challenges, content gaps and rate limits per platform.
- Acceptance: every platform has a capability-matrix entry and reproducible smoke-test record; unavailable credentials are marked as external validation blockers, not silently claimed as passing.

### Phase 6 — Release readiness

- Security and privacy review, backup/restore and upgrade documentation.
- Complete notices, license review and GitHub push checklist.
- Acceptance: a clean clone can install, test and run from the documentation; no secret or runtime data is tracked.

## Definition of done

The goal is complete only when all implementation phases pass their automated acceptance checks and the seven-platform qualification matrix truthfully records live outcomes. Login challenges requiring a human scan or credentials remain explicit user-assisted verification steps.
