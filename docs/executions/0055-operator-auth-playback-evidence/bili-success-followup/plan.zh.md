[English](plan.md) | **中文**

# 冻结实施计划

1. 先提交这些双语记录，再修改代码；不改锁定上游及用户数据。
2. 控制台：精确账户对应的已认证状态显示中性的“无需启动登录预检”，提供刷新本地状态和订阅入口，明确不是实时平台探测。跳过自动/手动启动预检，清除或隐藏旧失败并隔离迟到响应。保留真正不支持、失败、过期与忙碌状态；按后端明确资格收紧扫码启动门槛，不改后端认证转换。
3. 仅 BILI runner：原 update_cookies 完成后，调用捕获的原 pong 一次。只有 `is True` 成功；False 以现有固定 failed 终止；非布尔为 configuration_invalid，普通异常仍为 failed。不能返回爬虫继续运行、重试或扫码回退。其余六平台和初始 saved-session pong 保持不变，沿用父进程超时、取消及进程树控制。这仍是现有上游 pong 的证明强度，不是未来严格粘贴 Cookie 校验器。
4. 回归：已认证、状态缺失/错账户、过期、旧预检 UI；B 站初始 false、更新、第二次 false/true/异常形状/异常/取消；其他平台兼容、超时清理和 API 映射。测试和记录不含真实秘密。
5. 只读检查现有生产控制台。用户指定的验证作者 UID 252671524，需先确认具体全历史和调度范围再采集。作者预览只是本地语法/策略校验，不能当远程证明。不以“一条”承诺掩盖完整历史扫描，不代替用户接受许可证或建立非预期周期任务。
6. 运行并记录 Python、Web、文档和上游验证；独立复核；记录已实现/待实现并双语提交、推送核验。生产部署由用户控制，真实复用、采集、播放没有实测就保留 NOT_RUN。

