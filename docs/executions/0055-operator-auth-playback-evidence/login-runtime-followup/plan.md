**English** | [中文](plan.zh.md)

# Login integration follow-up plan

- Date: 2026-09-05
- Status: Frozen before implementation

1. Record the operator Linux smoke and read-only browser observations without private URLs, full account/session identifiers, cookies or logs. Preserve existing frozen plans.
2. Verify PyExecJS startup selection using the pinned public dependency. Install Debian Node.js in the final container, not pnpm/Web build tooling. Record its runtime version. Extend the existing isolated Python doctor probe with a bounded fixed JavaScript arithmetic execution; no upstream import, platform request, credential, profile or automatic license acceptance. Preserve fixed safe error codes and the existing subprocess timeout/environment.
3. Normalize upstream QR inputs: retain bounded byte input compatibility; accept strict bounded base64 and explicitly allowed base64 raster-image data URIs, reject URLs, malformed/oversized payloads and unsupported types. No network fetch or image viewer. Keep atomic confined writes, permissions, lifecycle cleanup and non-fatal relay behavior. Do not log input.
4. Add real pinned QR-helper contract tests using fake browser/network boundaries, normalization/redaction/size tests, and doctor dependency tests. Review exact Docker wiring; no Docker executable is available locally, so image build remains operator-run.
5. Integrate with the ongoing login-diagnostics increment, run relevant Python/Web/quality gates, record actual failures and reruns, make bilingual explicit Git commits and push. Request an updated-image doctor and a single operator-led QR result; retain live failure/unknown until verified.
