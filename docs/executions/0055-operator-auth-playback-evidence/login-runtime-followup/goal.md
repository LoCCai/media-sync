**English** | [中文](goal.zh.md)

# Login integration follow-up goal

- Date: 2026-09-05
- Status: Frozen before implementation
- Context: Published runtime repair `11dbd06`; diagnostics plan `488ce20` is being implemented in parallel.

Close two source-backed omissions found after the operator supplied a passing Linux blank-browser smoke and fresh Bilibili/Douyin logins still failed: the final container lacks the JavaScript runtime required by upstream startup; the QR relay drops upstream base64 strings. Preserve pinned sources, credentials, auth/process fences and the seven-platform/subscription/archive/Emby goal. Do not claim that either omission proves the recorded generic nine-second failure without deployed evidence.

The user additionally requested pasted-Cookie validation and persistence. That is accepted follow-up scope with its own security/implementation plan; it is not silently included in QR changes or represented as complete here.
