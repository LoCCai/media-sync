[English](plan.md) | **中文**

# 登录浏览器运行环境计划

- 日期：2026-09-05
- 状态：实现前冻结

1. 先提交本轮双语目标/计划/推进/验证基线，保留此前冻结计划。
2. 登录、作者和详情共用批准的浏览器子进程环境构造，包含 `PLAYWRIGHT_BROWSERS_PATH`；不透传代理凭据、任意应用秘密和 Python 注入设置，保留必要 Windows/显示设置。
3. 对锁定上游常规启动增加显式 bundled Chromium 适配策略，仅移除冲突的浏览器选择项，保留 profile、proxy、上下文选项；接入登录、作者、详情入口。不改上游源文件、不宣称指纹兼容已真人验收。
4. 登录预检匹配子进程环境，运行有界的临时有头持久浏览器。保留通用无头 readiness，错误输出为安全固定值。不访问账户 profile 或平台网址。
5. 后台 check-config 之后、数据库初始化之前，增加有界 Xvfb 就绪/存活检查。保留 help/check-only 路径，验证早期失败不迁移，不放宽凭据权限。
6. 跑针对性和更广回归、静态/类型/文档/上游检查，独立复核启动策略。本地现有运行时支持时验证真实空白浏览器；Linux 镜像和真人平台未执行就标 NOT_RUN。
7. 保存双语推进、实际命令/结果、待验证项和准确重建步骤；明确暂存审阅文件，双语提交并推送 GitHub，确认远端身份和清洁状态。不自动部署，不宣称总体目标完成。

预检以外仍缺少可操作运行时异常分类的部分明确留作后续，禁止暴露上游原始日志。修复打包链路后，先由操作者配合验收一个扫码登录及会话复用，再扩大真人 canary。
