[English](plan.md) | **中文**

# 执行 0040 计划

- 状态：已执行（实现）；完整门禁移交 Linux
- 日期：2026-09-03

## 交付顺序

1. 在 FastAPI 应用工厂中镜像 CLI 控制面，复用 CLI 的 payload 构造器与 worker 组装助手，保证 API 与 CLI 输出逐字节一致。
2. 增加带独占键的进程内操作注册表（登录/导出），重复启动返回 409，控制台可轮询结果。
3. 为登录子进程的 QR 钩子增加部署可用的文件中继与逐尝试清理；保持浏览器内展示的默认行为不变。
4. 提供零依赖中文单页控制台，并新增 `media-sync serve`。
5. 新增离线 API 契约测试；本机跑静态门；完整套件留给 Linux 主机。

## 风险与回退

- API 只在 CLI 已有权限之处提供能力，且默认只绑回环；回滚即删除 `api.py`、`console.html`、`serve` 命令与 QR 中继块。
