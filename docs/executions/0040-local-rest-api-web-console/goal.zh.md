[English](goal.md) | **中文**

# 执行 0040 目标

- 状态：实现完成；运行验证限定在 Linux 部署主机
- 日期：2026-09-03
- 前置：执行 0039 实现（同一工作会话）
- 范围：本地优先的 REST API 与内置中文 Web 控制台，镜像有界 CLI 控制面，并包含面向容器显示的 QR 中继扫码登录

## 目标结果

1. `src/media_sync/interfaces/api.py` 在 `/api/v1` 下提供健康/就绪/设置、账户增查与登录状态、以后台操作形式运行的阻塞式 QR 登录、订阅增查与暂停/恢复/立即运行、调度 tick/run、pipeline run、调度任务、资产、Emby 导出与操作注册表；长操作在受控后台线程执行，通过 `/api/v1/operations` 轮询。
2. `media-sync serve` 以 `MEDIA_SYNC_API_HOST/PORT`（默认 `127.0.0.1:8632`）启动 uvicorn，并在 `/` 提供内置控制台。
3. 登录子进程把二维码挑战图镜像到 `<account_root>/login-qr.png`（0o600 原子写、尝试结束后删除），容器部署可在 Web 控制台展示二维码而浏览器运行在 Xvfb 上；不启用中继时二维码保持在浏览器内，与既有行为完全一致。
4. 控制台（`console.html`）为免构建中文单页，覆盖账户、带实时二维码/状态轮询的扫码登录、订阅、带双重 MediaCrawler 门禁的 tick/同步/pipeline 运行、任务、资产、操作记录与 Emby 导出。
5. 不新增鉴权：API 默认只绑定回环地址，文档要求仅发布到可信网络。

## 验收边界

- 不新增凭据面、远程爬取能力或证据规则变更；每个端点复用 CLI 的服务层与脱敏 payload。
- QR 中继仅在既有 0o700 账户根内写一个图像文件，并随尝试删除；绝不改变登录结果。
- 新增离线 API 契约测试（`tests/unit/test_api_server.py`）；完整套件在 Linux 运行。

## 明确延期

Docker 打包属执行 0041。真人 QR 登录、真人抓取与真实下载验收仅在操作者授权的部署执行中进行。
