**English** | [中文](plan.zh.md)

# Frozen plan

1. Reuse the locked Weibo client in an independent supervised profile worker, not crawler.start/core creator traversal/store. One GET /api/config must explicitly return login=true before one get_creator_info_by_id request to the exact /api/container/getIndex query (jumpfrom=weibocom, type=uid, value=UID, containerid=100505UID). Enforce raw ok=1, userInfo.id identity equality, bounded nickname, optional avatar. Reject public profile data without fresh authenticated evidence, changed/ambiguous queries, redirects and retries. Ignore incidental cards in the bounded profile response; do not request posts/feed/comments/media.
2. Retain shared account locks, private framed Cookie transfer, checkout-origin validation, parent-death/timeout/tree cleanup and 45-second execution plus up to 15-second cleanup. Saved-session uses only its existing scoped profile; Cookie uses the complete parsed candidate in a fresh nonpersistent context, with no fallback to old browser state. Block browser network and serve an empty same-origin document. No QR or account authentication promotion. Exact network destinations, public DNS pinning, no proxy inheritance and bounded JSON remain mandatory.
3. Extend the existing numeric identity contract to wb without weakening Bili or permitting the other five platforms. Canonical homepage is https://weibo.com/u/UID. Keep account/platform/creator/operation/generation/auth-revision binding and atomic profile plus Operation success. No migration, historical author rename or export-path change is required.
4. Extend optional avatar retrieval with a conservative HTTPS Weibo CDN host/path allowlist, no credentials, queries, redirects or arbitrary targets. Reuse bounded isolated download/decode and same-origin static PNG. Unknown URL shapes or image failures preserve text and any prior avatar; do not claim all remote image forms are supported. Keep existing Bili avatar API compatibility.
5. Enable WB lookup in the actual subscription controller/UI for saved-session and Cookie, independently of full-history capture acknowledgement. Keep one request per settled identity, account/platform/generation/session fencing, safe canonical URL parsing and manual retry. Accurate text must still mark five platforms as not yet connected; auth/profile success is not capture or playback qualification.
6. Validate locked real client methods against mocked bounded transport; strict response/identity/query/attempt rejection; both browser-context modes and forbidden calls; shared process gates; service/API/DB receipt and cross-platform/account isolation; avatar security; front-end automatic entry/late response handling. Run targeted regressions plus proportionate full static/Web/docs/upstream/package checks, recording actual commands, failures, corrections and environmental/live NOT_RUN gates. Commit bilingual plan before dependent edits, bilingual implementation and progress, non-force push and fresh-fetch verification.

No production login, Cookie entry, subscription mutation, retry, capture, download/export, deployment, media-server action or supervisor restart is authorized by this coding execution. Remaining DY/KS/Tieba Cookie validators must first establish authenticated-self evidence; local Cookie flags or public/relationship data cannot be relabeled login proof.

