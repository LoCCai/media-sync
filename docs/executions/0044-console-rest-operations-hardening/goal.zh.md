[English](goal.md) | **中文**

# 执行 0044 目标

- 状态：由执行 0048 重新定为最小运维恢复切片；UI 强化延期至 0.2
- 日期：2026-09-03（改范围）
- 前置：执行 0042（完成度归档）
- 范围：操作者在真人验收期间真正需要的运维端点——带近期任务的订阅详情、调度任务详情、经既有下载服务的资产重下载——以及驱动它们的最小控制台改造

## 目标结果

1. `GET /api/v1/subscriptions/{id}`：调度状态、近期同步运行（状态/计数/时间）与近期调度任务，按 CLI 脱敏规则投影。
2. `GET /api/v1/scheduler/jobs/{id}`：单条脱敏 Job 投影。
3. `POST /api/v1/assets/{id}/download`：以受控后台操作为单个资产运行既有 `AssetDownloadService`（与 CLI `asset download` 同一服务），遵守 pipeline 能力门；不走重置/CAS 捷径，除服务自身受 fencing 保护的生命周期外不做任何状态修改。
4. 控制台：订阅详情抽屉（调度 + 近期任务 + 近期运行）与逐资产的重下载按钮。
5. 离线 API 测试扩展 `tests/unit/test_api_server.py`；不新增权限、凭据面或 schema。

## 验收边界

- 只读详情端点 + 一个走服务的下载触发；无删除/清理，UI 不做 lane 编辑。
- 延期至 0.2：统一运维仪表盘、UI 任务控制按钮（resume/cancel）、超出 payload 投影的故障诊断视图。
- 完整套件在 0048 校准运行处执行；真人行保持 `NOT_RUN`。

## 明确延期

验收边界所列各项，外加鉴权/多用户/远程加固。
