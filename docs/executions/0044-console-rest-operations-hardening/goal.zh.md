[English](goal.md) | **中文**

# 执行 0044 目标

- 状态：计划中——先提交启动记录供评审，再实现
- 日期：2026-09-03
- 前置：执行 0042（完成度归档）
- 范围：对执行 0040 的 Web 控制台与 REST API 做运维强化——只读的订阅/账户详情、带逐任务操作的调度任务详情、资产重下载触发、统一运维视图——补齐对 bili-sync-up 任务管理工作流的剩余差距

## 目标结果（预期）

1. `GET /api/v1/subscriptions/{id}` 详情（调度、近期运行、近期任务）与 `GET /api/v1/scheduler/jobs/{id}` 详情（脱敏 payload 投影）。
2. `POST /api/v1/assets/{id}/redownload`：经既有 fenced CAS 路径重置已验证资产并入队 pipeline 协调器——复用而非复制执行 0005 的恢复语义。
3. 控制台：任务详情抽屉、带重下载按钮的资产状态、合并操作历史/lanes/调度控制的运维页。
4. 每个端点仍是既有服务之上的薄投影；不新增权限、凭据面或 schema。

## 验收边界

- 仅只读或恢复路径操作；本执行不提供删除/清理端点。
- 离线 API 测试扩展 `tests/unit/test_api_server.py`；完整套件在 Linux 部署主机运行；不做本机部署验证。
- 真人行保持 `NOT_RUN`。

## 明确延期

鉴权/多用户、远程访问加固、UI 配置编辑。
