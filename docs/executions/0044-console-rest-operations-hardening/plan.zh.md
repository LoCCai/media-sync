[English](plan.md) | **中文**

# 执行 0044 计划

- 状态：计划中
- 日期：2026-09-03

## 交付顺序（实现时）

1. 详情端点（订阅、调度任务）投影既有仓储/调度数据，使用 CLI payload 已有的脱敏规则。
2. 资产重下载端点组合既有“重置 + 入队”路径；契约测试证明一次重置恰好产生一个 pipeline 协调器。
3. 控制台新增（详情抽屉、重下载、运维页）对接相同端点。
4. 扩展 `tests/unit/test_api_server.py`；在 Linux 部署主机运行全部质量门族，收尾前记录准确数字。

## 风险与回退

- 仅新增端点；回退即删除新路由与控制台区块，不影响既有行为。
