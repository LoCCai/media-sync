[English](plan.md) | **中文**

# 执行 0044 计划

- 状态：计划中
- 日期：2026-09-03

## 交付顺序（实现时）

1. 详情端点（订阅、调度任务）投影既有仓储/调度数据，使用 CLI payload 已有的脱敏规则。（0048 改范围：本项与第 2/3/4 项即全部范围。）
2. 资产下载端点以受控后台操作为单个资产运行既有 AssetDownloadService——与 CLI asset download 命令驱动的是同一服务；测试覆盖操作生命周期与门禁失败。
3. 控制台新增（详情抽屉、重下载、运维页）对接相同端点。
4. 扩展 `tests/unit/test_api_server.py`；在 Linux 部署主机运行全部质量门族，收尾前记录准确数字。

## 风险与回退

- 仅新增端点；回退即删除新路由与控制台区块，不影响既有行为。
