**English** | [中文](goal.zh.md)

# Goal

Make Bili ordinary-upload creator subscriptions genuinely bounded and resumable through the actual scheduler/capture/ingestion pipeline. A small max_items must bound fetched details, retain unconsumed discovered items across jobs, and never let an incomplete newest-first crawl advance a watermark that hides older history. Keep head refresh and historical scanning progressing fairly; show completed work separately from coverage. This is the next dependency of the failed live canary, not a claim that its historical schema_invalid root cause has been proved.

Baseline `6eabc74`, freshly fetched with clean worktree and HEAD/origin divergence 0 0. Previous goal turn is progress: 0058 implemented/published four Cookie validators with complete-directory tests and package/source checks. The full seven-platform login/subscription/text-image-video/archive/local Emby/Jellyfin goal is unchanged. Bili dynamic attachments, other platform gaps and live qualification remain required; this upload slice must not be presented as total Bili content support.
