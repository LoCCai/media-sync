**English** | [中文](plan.zh.md)

# Frozen plan

1. Commit this plan before implementation. Keep locks/dependencies/schema unchanged. Implement original protocol facts from [sources](sources.md), reusing real locked methods without copying upstream code/response fixtures. Separate platform modules; root owns shared identity/worker dispatch/avatar boundaries/API/UI and end-to-end receipts.
2. Douyin entry lookup_douyin(checkout, profile, remote_id, deadline, optional cookie): exact saved session or fresh Cookie context, only a locally fulfilled fixed-origin blank page for required localStorage/UA, all other browser traffic blocked. Bind execjs to Node before actual import; execute locked get_user_info/get/parameter processing/JS signing. One fixed HTTPS profile/other GET; validate identity, fixed parameters, headers and real a_bogus, bounded pinned transport with no redirects/proxies. Require returned string user.sec_uid==input and raw bounded nickname. Integer status_code0 is our conservative success subset, not proof all source responses have that type. No pong/self/content calls or auth mutation; unknown signing/response failures stay fixed/private, never invent tokens.
3. Tieba entry lookup_tieba with the same signature: real get_creator_info_by_url→_fetch_json_by_browser→_sign_pc_params; a one-call Page adapter fences the sole signed GET /c/u/pc/homeSidebarRight. Exact portrait/un/subapp_type/_client_type/sign, no goto/tbs/feed/extra evaluate; bounded pinned HTTP. Strictly parse JSON before upstream parsing to reject duplicate keys/NaN/implicit missing-field success. Require integer error_code0 and integer no0 if present. The extractor accepts only the exact returned object and yields our raw profile DTO.
4. Identity: exact DY ASCII [A-Za-z0-9_-]{1,255}; Tieba bare modern tb.1. portrait with28–31 suffix characters, no repeated/trailing dots. Returned portrait is either exact input or input plus one ?t=10 ASCII digits; strip only that proven suffix before exact binding. No aliases/numeric substitution/arbitrary query/input fallback. Prefer raw name_show, falling back to name only for missing/empty value, not invalid types or str coercion. These are application safety subsets. Fixed homepages use douyin.com/user/ID and tieba.baidu.com/home/main?id=portrait.
5. Tieba avatars accept only source-backed exact https://gss0.bdstatic.com/6LZ1dD3d1sgCo2Kml5_Y_D3/sys/portrait/item/{returned portrait}, optionally with the proven ?t=timestamp. Bind the same portrait; unknown img_url is not fetched and no wildcard CDN extension. Reuse isolated bounded no-Cookie download/decode, same-origin PNG/CSP, optional failure/previous-avatar retention. DY avatar URL shapes remain unproven and pending.
6. Expand to six-platform automatic nicknames with exact inputs/homepages/identities/results/receipts and account-page copy. Optional avatars for Bili/WB/Tieba, explicit nickname-only elsewhere. Refresh never renames Author/history/export. Correct obsolete unsupported tests while keeping unknown-platform rejection and numeric-ID authority. Standalone lookup needs no full-history capture acknowledgement; capture keeps its separate gate.
7. XHS locked/historical source proves a nickname field but still fills identity from input, not the response. Do not open success or add empty scaffolding. Further source discovery remains required; six platforms are not seven-platform completion. DY/KS pasted self validators and remaining avatars/media/live qualification stay open.
8. Test actual locked methods with synthetic HTTP/browser boundaries: signatures/fixed budget/Cookie isolation, duplicate JSON/invalid Unicode/wrong identity, safe optional avatar failures, private process frames, API/subscription receipts/path retention/late results. Run focused tests during edits, then frozen-source complete regression, Web/static/docs/locks/package audits. Record actual failures, exact commands/counts/skips; do not sum overlapping selections. Bilingual commits, normal push/fetch equality and separate publication record. Current Linux/real PG/platform/CDN/playback remain NOT_RUN unless executed.

