[English](goal.md) | **中文**

# 执行 0036 目标

- 状态：冻结的离线微博视频封面范围已完成；真人行保持 `NOT_RUN`
- 日期：2026-09-03
- 前驱：执行 0035 收尾 `5a27e99949c54a5032454d91b8809d28afad7086`
- 范围：普通原创微博视频帖的封面——`page_info.pic_info`——与 0031/0035 的流 URL 一同捕获，交付为一个带适配器刷新与 Emby 封面发布的 COVER 资产
- 计划提交：`1ad49a7`
- 实现提交：`72e9f62`

## 结果

1. 为锁定微博 store shim 的视频捕获扩展封闭封面通路：`page_info.pic_info.pic_big.url` 必须是 `sinaimg.cn` 族 host 上带界路径的 HTTPS 静态图片 URL；视频形状（标量或 `playback_list`）必须先成功捕获，无合法封面的视频仅捕获视频。
2. 封面置于一个新的私有 `{"url"}` 字段下：与全部既有微博私有字段严格防碰撞、持久化前递归移除，归一化在 VIDEO 资产旁物化既有 `{note_id}:cover:0` COVER 资产。
3. 把 `AssetKind.COVER` 加入 WB 刷新支持集合，使封面像视频一样经一次精确 numeric-note detail 子进程重新解析，路径漂移关闭失败。
4. 封面发布为 Emby 剧集 poster，视频保持主媒体，零工作重放且持久不保留签名 query。
5. 通过真实子进程证明契约组合（标量与 playback-list 视频旁的封面、封面漂移关闭），外加一条 SQLite → 刷新 → 双资产下载 → 归档 → Emby 重放的集成组合。

## 验收边界

- 只有精确的 `pic_info.pic_big.url` 标量授予封面；URL 后缀、响应 MIME 与下载字节都不能影响它，无可捕获视频的封面被忽略。
- 无 `pic_info`、嵌套形状畸形、外域 host 或非静态扩展的帖子仅捕获视频，与 0031/0035 字节级兼容。
- 无数据库 schema 或迁移；稳定 Asset 身份不变。`.upstream` 保持只读且不入库。

## 明确延期

其他封面尺寸（`pic_orig` 等）、GIF/动图封面、转发、直播/付费媒体及全部真人验收行均不属于本执行。
