# Execution 0001 plan / 执行 0001 计划

1. Inspect workspace and installed Python, Node.js and Git versions. / 检查工作区与 Python、Node.js、Git 版本。
2. Clone both requested repositories into `.upstream/`. / 将两个指定仓库克隆到 `.upstream/`。
3. Record repository URL, branch, commit and license. / 记录仓库地址、分支、提交和许可证。
4. Define the non-vendoring boundary and delivery roadmap. / 明确不内嵌上游的边界和交付路线。
5. Verify JSON, links, ignored paths and Git diff. / 验证 JSON、链接、忽略路径与 Git 差异。
6. Create a bilingual local commit. / 创建中英双语本地提交。
