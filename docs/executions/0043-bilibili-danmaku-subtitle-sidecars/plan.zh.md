[English](plan.md) | **中文**

# 执行 0043 计划

- 状态：延期至 0.2；保留为下一周期启动计划
- 日期：2026-09-03

## 交付顺序（实现时）

1. 把严格 Bilibili 详情协议（v7+）扩展为从既有已获取播放 payload 读取封闭的弹幕/字幕描述符；签名 URL 仅运行时，持久化前递归剥离。
2. 实现纯离线 XML→ASS 转换器（冻结规则集、有界输出）；对生成夹具做单元测试。
3. 在导出布局中以同主干文件派生 sidecar；把零工作重放/恢复与留存树扫描扩展到 sidecar。
4. 新增契约 + 集成组合（下载 → 归档 → 含 sidecar 的 Emby），在 Linux 部署主机运行全部质量门族；收尾前在此记录准确数字。

## 风险与回退

- sidecar 是衍生产物：回退即删除转换器、描述符与布局输出，不触碰媒体身份或 schema。
