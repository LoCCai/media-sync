[English](goal.md) | **中文**

# 执行 0043 目标

- 状态：计划中——先提交启动记录供评审，再实现
- 日期：2026-09-03
- 前置：执行 0042（完成度归档）
- 范围：Bilibili 弹幕（XML → ASS）与字幕 sidecar 随剧集媒体下载并发布进 Emby 目录，复刻 bili-sync-up 对既有 Bilibili 形状的边车覆盖

## 目标结果（预期）

1. 详情协议在既有已授权播放页 payload 上增加有界、封闭的弹幕/字幕元数据读取；签名 CDN URL 保持仅运行时。
2. 弹幕 XML 以固定规则集（字体、字号、滚动/静止泳道、有界条数）转换为 ASS，字幕保留原生格式；两者以同主干 `.ass`/`.srt` sidecar 写入 Emby 布局中剧集旁。
3. 下载遵循与媒体相同的签名 URL、字节上限与归档纪律；sidecar 是衍生产物而非版本化 Asset，冻结媒体形状计数不变。
4. 零工作重放与恢复行为扩展到 sidecar，不新增数据库 schema。

## 验收边界

- 仅既有已交付的 Bilibili 形状（progressive/DASH/FLV、1–64 分 P）；不含番剧/直播。
- 仅离线夹具；全部真人 API/CDN 行 `NOT_RUN`（操作者，执行 0047 或部署主机）。
- 完整套件与新增测试在 Linux 部署主机运行；不做本机部署验证。

## 明确延期

弹幕样式配置、字幕翻译/合并、其他平台的弹幕。
