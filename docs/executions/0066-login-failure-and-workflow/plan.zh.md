**中文** | [English](plan.md)

# 冻结计划

1. 保留干净基线 `ddf9477` 与上游锁定，应用修改前提交计划。不部署、不发起登录、不读取 Cookie、不恢复 supervisor、不生产批量采集。
2. 生产只读证据：用户报告的抖音操作运行约 46 秒，在 `authenticating` 阶段结束，保存 `operation_login_failed` 和 `runner_status=failed`。不能据此判断浏览器、网络或凭据根因，不修改历史证据，也不根据时长反推原因。
3. 新增封闭运行结果，区分上游 `SystemExit`、Playwright 类型化超时、现有本地登录确认拒绝信号。保留父进程总期限、过期、浏览器启动、取消、未知失败及进程所有权语义。不序列化异常消息、Cookie、URL 或堆栈。上游二维码辅助函数会吞掉部分异常，明确保留此限制。
4. 打通严格进程帧、持久摘要、精确会话诊断、账户和任务页解释。兼容旧记录、拒绝未知值；对已保存的通用失败不再误称未保存诊断，提供安全定位路径，避免引导盲目反复登录。
5. 核查实际订阅、常驻调度、下载、本地媒体库链路，以及历史覆盖和目录设置。记录已实现和待实现；正常流程不再要求手动发布，兼容保留 API 和内部导出标识。
6. 明确下一阶段顺序：可编辑总目录与平台覆盖，API/supervisor 共享持久配置及安全变更；精确订阅的一次性完整执行；历史完成与增量持久语义；用户确认后的限量真实验收。通过限速、退避和遇验证暂停降低风险，不绕过验证码、不轮换代理或账户、不承诺规避所有风控。
7. 运行受影响后端、前端、静态、文档和上游检查，如实记录失败及未运行项。中英双语提交并正常推送，核对远端一致。本阶段改进诊断和工作流，不声称真人登录已修好或历史下载已完成。
