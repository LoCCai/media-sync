**English** | [中文](plan.zh.md)

# Frozen plan

1. Preserve the clean `ddf9477` baseline and locked upstreams. Commit this plan before application changes. No deployment, new login, Cookie access, supervisor restart or production bulk collection.
2. Read-only production evidence: the reported DY operation lasted approximately 46 seconds, ended in `authenticating`, and persisted `operation_login_failed` with `runner_status=failed`. This does not identify a browser, network or credential root cause. Do not rewrite historical evidence or infer a cause from elapsed time.
3. Add closed runner outcomes for upstream `SystemExit`, a typed Playwright timeout and our existing post-login confirmation-rejection sentinel. Preserve parent deadline/expiry, browser-launch, cancellation, unknown failure and process ownership semantics. Never serialize exception messages, Cookie values, URLs or tracebacks. Upstream QR helpers swallow some exceptions; explicitly retain this limitation.
4. Carry the outcomes through the strict process frame, durable summaries, exact-session diagnostic and Accounts/Jobs explanations. Preserve old records and reject unknown values. Fix the misleading missing-diagnostic copy for a saved generic failure; give it a developer-safe correlation path instead of encouraging blind repeated login.
5. Audit the real subscription → supervisor → download → local library chain, historical coverage and directory settings. Record implemented versus pending behavior. Adjust normal-workflow guidance so manual maintenance publishing is not presented as a required user step; keep compatible API/internal export identifiers.
6. Freeze the next delivery work in priority order: editable shared/per-platform directories with shared API/supervisor persistence and safe changes; exact-subscription one-shot chain; durable historical completion and incremental behavior; bounded real canary with operator confirmation. No CAPTCHA bypass, proxy/account rotation or claims of guaranteed risk avoidance: use pacing, backoff and pause-on-verification.
7. Run affected backend and Web tests, static checks, documentation and lock checks; record failures and NOT_RUN honestly. Bilingual local commits and normal push, then verify remote equality. This milestone improves diagnostics/workflow and does not claim repaired live logins or completed historical downloads.
