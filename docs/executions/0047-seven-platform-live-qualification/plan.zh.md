[English](plan.md) | **中文**

# 执行 0047 计划

- 状态：等待操作者执行
- 日期：2026-09-03

## 操作者流程（每平台，部署主机上）

1. 前置门：`uv sync --all-groups --locked && uv run pytest -q` 通过；`docker compose up -d` 健康；控制台可达。
2. 登录：控制台扫码登录（或按账户偏好用 Cookie）；记录方式、挑战类型与耗时。
3. 订阅：每平台一个已知良好创作者、小 `max_items`；立即运行。
4. 同步：运行同步 worker；记录任务结果与发现条数。
5. 下载/导出：运行下载/导出 pipeline；记录资产数、归档字节、Emby 目录列表。
6. 再同步一次；记录增量性（第二次只发现新条目、零重复下载）。
7. 每平台在本目录记录一行（双语），更新能力矩阵与完成度归档，然后附离线套件数字收尾。

任何被阻塞平台记 `BLOCKED_EXTERNAL` 并写明原因（无账户、地区不支持、上游行为变化）——记录本身就是交付物，而不是“通过”。

## 回退

本执行不改代码；平台记录为只增文档。
