[English](plan.md) | **中文**

# 执行 0028 计划

1. 盘点语料：按混排约定（`EN / ZH` 行内对、标签对、加粗标签对、竖排段落/列表重复、语言中性的命令或表格证据）分类每行 Markdown，并在写任何转换之前量化歧义。
2. 编写带 `--dry-run` 与 `--write` 模式的 `scripts/split_bilingual_docs.pl`：无内部边界的反引号段先被掩蔽，使命令与引用的提交信息不会破坏识别；每个单元解析唯一有效的行内边界；标签对与加粗标签对先于普通拆分识别；竖排重复按类型与长度逐项配对；表格行逐格拆分；中文版把本地链接重写为 `.zh.md`，每份版本加语言切换行。
3. 先干跑、审查完整报告（竖排配对清单、长度不匹配、未配对单元、中性 ` / ` 行），修复工具后再写入。
4. 拆分后人工处理：补回旧约定只放在单侧的提交信息、折叠同文对、修复五处历史翻译缺口，并补译单语言剩余内容。
5. 用实现 `scripts/check_docs.py` 链接契约外加成对、纯度、中性行保全与未拆分检查的 Perl 门禁验证，因为迁移工作站没有 Python 运行时。
6. 更新日志（本目录 README、执行索引；除 0028 记录外 roadmap 不动），以两种语言创建 0028 四件套记录，并把 `uv run python scripts/check_docs.py` 与常规质量门留给推送前复跑。

## 风险与回退

- 转换就地改写被跟踪文档；开始前工作区干净，因此 `git checkout -- "*.md"` 并删除未跟踪的 `.zh.md` 即为精确回滚。
- 误分类风险由干跑报告、四项验证门禁与“无运行时产物依赖文档正文”这一事实共同约束。
