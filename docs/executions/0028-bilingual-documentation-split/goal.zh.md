[English](goal.md) | **中文**

# 执行 0028 目标

- 状态：文档语言范围已完成；运行时行为零变化
- 日期：2026-09-02
- 前置：执行 0027 收尾 `539395a483f4f368895fb0f35205c903dc2bd43d`
- 范围：把所有中英混排 Markdown 文档替换为两份单语言版本——英文保持原路径 `<name>.md`，中文变为同目录 `<name>.zh.md`——不改变任何源代码、schema 或运行时行为

## 目标结果

1. 仓库根目录下每份 Markdown 文档（生成的 `.zh.md` 除外）处理为原路径英文版与 `<name>.zh.md` 中文版。
2. 每份版本顶部带指向对应版本的语言切换链接，版本内的本地 Markdown 链接指向同语言文件。
3. 混排写作约定（`EN / ZH` 行内对、标签对、加粗标签对、竖排重复段落）由可提交、可重跑的迁移工具确定性解析，而不是人工改写 124 份文件。
4. 此前只有单一语言的内容（未翻译的 requirements 条目、research/architecture 段落、决策与上游说明）被补译，使两份版本内容完整。
5. 验证证明链接完好、中英文件成对、英文版语言纯净、中性行零丢失、中文版无残留未拆分配对。

## 验收边界

- 仅涉及文档与迁移工具；`src/`、`tests/`、`alembic.ini`、`pyproject.toml` 与 `upstreams.lock.json` 不变。
- 反引号内引用的 Git 提交信息在两份版本中原样保留；它们是历史产物而非文档正文。
- 迁移工作站没有 Python 运行时，因此 `uv run python scripts/check_docs.py` 与常规质量门在该机为 `NOT_RUN`，推送前必须在常规环境复跑。
- 历史 `NOT_RUN`/延期声明原样复制进两份版本；不重新验收任何执行边界。

## 明确延期

真人平台/CDN/Emby 验收、REST/生产打包、多 `durl` 分段及其他产品项均不在本执行范围内。
