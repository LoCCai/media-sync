[English](progress.md) | **中文**

# 执行 0028 推进结果

- 状态：文档语言范围已完成
- 日期：2026-09-02

## 已完成

- 完成 124 份 Markdown 的语料分析：3371 行恰好携带一个有效行内 `EN / ZH` 边界、0 行多边界、21 行需特殊处理——证明确定性拆分可行。
- 实现带干跑/写入模式、反引号掩蔽、首有效边界解析、标签/加粗标签处理、成组竖排配对、逐格表格拆分、中文链接重写与语言切换行的 `scripts/split_bilingual_docs.pl`。
- 三轮干跑/修复发现并修正：标签对顺序 bug（标签从未生效）、取末边界而非首边界的 bug（加粗验收行保持混排）、以及丢失 `— PASS.` 后缀的 Perl 捕获变量 bug。
- 最终写入：3933 处行内拆分、356 个标签对头部、531 个竖排配对单元、124 份英文版就地改写、124 份中文版新建。
- 人工处理：执行 0020–0023 计划的提交信息补回两侧；折叠同文对（`Ruff / Ruff`、`Pytest cases / Pytest case`、`SQLite sidecars / SQLite sidecar`、`Creator/Detail child shim`）；0009 计划的索引单元格按全角分号拆分；0009 冻结契约段落补上缺失的英文对应；日志 README 的中文导航链接重建。
- 补译：34 条 requirements 需求、两份 research 文档、11 段 architecture（含把一段合并译文拆回两段）、5 条决策、上游说明及零散标签内容补入中文版；roadmap 中文标题恢复阶段编号。
- 更新日志：双档布局说明、重写的文档规则、双语执行 0028 索引行与摘要段。

## 偏差与决策

- 迁移工作站（Git Bash + Perl 5.42）没有 Python/uv 运行时，无法执行常规质量门；改用 Perl 门禁复现链接契约并新增成对/纯度/保全检查。此差异记录为 `NOT_RUN`，不记为通过。
- 反引号内引用的提交信息在两版保持双语：它们是逐字历史产物。

## 待完成

- 推送前在常规 Python 环境复跑 `uv run python scripts/check_docs.py`、Ruff、mypy、完整测试套件与仓库审计。
- 后续里程碑必须按更新后的文档规则直接撰写两份版本。
