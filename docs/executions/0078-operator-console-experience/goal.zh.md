[English](goal.md) | **中文**

# 执行 0078 目标

- 状态：进行中——实现启动记录（本会话由操作者授权；独立于冻结中的 XHS/抖音链）
- 日期：2026-09-23
- 前置：执行 0077 收尾（`main` 上 `3c31dce` 系列）
- 范围：操作者控制台体验——弥合部署交接文档与 media-sync 控制台在原生登录期间实际展示内容之间的缺口：内嵌实时二维码面板、crawler 运行日志进 media-sync 日志中心、产物位置总览；外加文档债清理（status 矩阵刷新到 0077 边界、执行索引回填）

## 目标结果

1. **内嵌二维码面板**（账户区）：在预期进行原生登录期间，以短间隔轮询已鉴权的 `/crawler/api/crawler/qrcode`；200 时显示图片，404 时区分「等待生成 / 已过期（180 秒） / 平台选择器失败」并给平台特定指引（抖音 → 按 0065 流程改用 Cookie 粘贴）。只读；不新增端点或凭据面。
2. **crawler 日志桥接**：WebUI manager 已捕获的脱敏 crawler stdout 行，额外有界、脱敏、带来源标记地写入既有 media-sync 滚动日志中心，使控制台 → 日志能紧邻 media-sync 自身操作看到原生 crawler 运行；`/crawler/` 页的实时面板保持不变。
3. **产物位置总览**：信息卡（库/设置区）解释三层产物——爬虫原始输出（`webui-output`）、校验过的 SHA-256 正本库（`/data/archive`）、Emby/Jellyfin 树（`/data/library`）——数据取自既有 settings API 的实际配置根。仅 UI。
4. **文档债**：`docs/status.md`/`.zh.md` 的里程碑、验证矩阵与发布阻塞块从 0075 冻结刷新到 0077 边界（叙事含义不变）；`docs/README.md`/`.zh.md` 执行索引按已记录的状态叙事回填缺失的 0059–0068 行；新的双语操作者速查清单 `docs/handoff-checklist.md`/`.zh.md` 加入日志导航。

## 验收边界

- 遵守 0077 冻结：本执行不改动任何 XHS/抖音投递逻辑、契约或语义；二维码轮询是对既有鉴权路由的只读复用。
- 不新增权限、schema 迁移或凭据面；日志桥接复用既有脱敏路径并遵守既有分段/总量预算。
- 仅离线验证（组件测试、桥接单元测试、静态门、编写工作站完整套件）；真人行为仍属操作者金丝雀范围。

## 明确延期

抖音 source-first 投递设计（以 XHS 金丝雀结果为门槛）、知乎头像 CDN 扩展、播放证据确认 UI、弹幕/字幕（0.2）。
