**English** | [中文](plan.zh.md)

# Frozen plan

1. Keep locks/dependencies/database schema unchanged. Implement original protocol facts from [sources](sources.md), not copied upstream implementation or third-party response fixtures. Split new platform lookup code into dedicated modules; root owns shared runner/identity/repository/API/Web wiring, agents own disjoint platform modules/tests.
2. Tieba: use the locked client/get plumbing for exactly one GET `https://tieba.baidu.com/mo/q/newmoindex?need_user=1`, with the candidate Cookie unchanged and no target user. Require explicit successful envelope and positive bounded immutable self ID plus valid nonempty returned portrait from the self endpoint. Missing/guest/ambiguous fields never authenticate. Known HTTP401 may reject; unproven platform error codes are result_invalid rather than invented auth-expiry semantics. No local BDUSS marker, successful HTTP alone, arbitrary profile retrieval or pong qualifies login. Reuse existing secret-safe guardian, account exclusion, private immutable candidate storage and atomic Account/Operation success; candidate failures preserve old credentials. Add fifth-platform capability/UI support only after actual runner wiring and tests.
3. Shared creator identity: retain canonical bounded uint64 numeric Bili/Weibo IDs. KS uses exact ASCII opaque `[A-Za-z0-9_-]{1,128}`; Zhihu uses exact ASCII url_token `[A-Za-z0-9._-]{1,255}` excluding dot/dotdot path segments. Bounds are application safeguards, not exhaustive platform claims. Validate platform-specific identities at request/result/repository/API/UI boundaries, derive fixed official homepages, retain account/generation/credential/lease/cancel/receipt fences and existing media/export paths. Never reinterpret an alias/returned numeric ID as an opaque ID.
4. KS: eligible locally authenticated saved-session or existing Cookie account, one locked `visionProfile` GraphQL operation with exact input ID/query and a bounded no-redirect/no-proxy pinned POST transport. Parse `data.visionProfile`, strict integer result1, exact string `userProfile.profile.user_id==input`, nonempty bounded `user_name`. Do not call relationship-list pong or mutate authentication. This is exact creator observation, not remote self-login proof; absent/changed identity fails closed. Preserve optional headurl only within the existing optional-avatar boundary; no unproven CDN fetch rule or avatar-completion claim.
5. Zhihu: fresh empty Cookie context or exact saved-session context, locked real Node signing with lowercase cookie/signature headers retained; one `/api/v4/me` self check without email/phone include, then one exact signed `/people/{token}` HTML request. Bounded strict JSON/HTML transport, no retries or browser feed navigation. Read exact `initialState.entities.users[token]`, require its own `urlToken==token`, raw bounded name; do not inherit extractor masking or fallback to input identity. Missing/renamed/redirected identity cannot publish. Optional avatarUrl is not permission to fetch an unknown CDN. Keep other page entities/incidental content out of results/storage.
6. UI: extend automatic one-shot lookup and strict result/homepage validation to both new platforms, preserve identity-change/late-result/cancel and receipt rules. Explain numeric UID versus KS ID/Zhihu token, nickname-only avatar boundary and that lookup does not crawl or require full-history acknowledgement. Tieba paste dialog uses the same secret-safe submission/clear behavior; no raw Cookie in logs/errors/history.
7. Verify real locked methods with synthetic transport/browser traps; reject wrong/missing identity, public-profile-as-self, malformed envelopes, extra calls/URLs, redirects/private hosts, oversized bodies, signed header loss, Cookie substitution, parser fallback and stale publication. Exercise application/repository/API/subscription receipt and frontend flows. Run targeted tests during edits, freeze source for final affected regression, separate directory snapshots/environment skips from latest-source evidence, audit package source inclusion and update bilingual status/deployment/capabilities/docs.
8. Commit the frozen plan before implementation; commit changes with bilingual explanations, non-force push GitHub, fresh-fetch equality, and record publication separately. Linux/real PostgreSQL/platform/CDN/playback are NOT_RUN unless actually executed; no production action is included. Continue remaining validators/profiles/avatars/media and live qualification afterward without redefining the seven-platform goal.
