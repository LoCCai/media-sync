**English** | [中文](progress.zh.md)

# Progress

Fresh baseline and prior0062 publication verified. Three independent read-only source audits make the next workflow actionable: Tieba self endpoint, KS exact visionProfile and Zhihu returned urlToken. Shared numeric-only profile contracts currently block KS/Zhihu. The frozen plan adds actual end-user paths while retaining honest missing-avatar and live gates. No implementation or new test PASS at plan freeze.

## Implemented and frozen

Tieba now uses the real locked get method for one pinned HTTPS self request, positive immutable ID/modern portrait checks and the existing private candidate/Account/Operation publication path. Five pasted-Cookie platforms are exposed end to end. Unknown server codes or response shapes remain fixed failures without replacing prior credentials. Strict integer no/id and modern portrait are our conservative acceptance subset, not proof that every legitimate upstream response has those exact types. No tbs probe, profile crawl or QR fallback was added.

KS and Zhihu exact nickname lookup now reach UI/API/repository and real isolated workers. KS executes the locked GraphQL/post sequence, validates the returned opaque ID and never calls pong or claims self authentication. Zhihu executes real locked Node signing, fresh self check and one exact HTML profile, requiring the returned row's own urlToken; arbitrary entities/avatar URLs are discarded. Both keep avatars absent pending source evidence. Account authentication is not mutated by creator lookup.

Shared profile identities/homepages are platform-bound through request, result, API, persistence and UI; KS128/Zhihu255 lengths are exercised, numeric Bili/Weibo authority is unchanged. Successful profile receipts create subscriptions without manually typing a nickname; later nickname updates preserve the original author/export path. Failed/stale results keep the prior observation. No full-history consent is needed for the standalone lookup; capture retains its separate platform gate.

Independent review found the API's old20-character cap, numeric-only UI hint and an escaped lone-surrogate name that could not encode as UTF-8; all are corrected with tests. Root reproduced a genuine script-mode integration defect: new modules imported a second copy of the shared DTO/exception classes, turning successes into result_invalid and fixed errors into temporary. Canonical script-module registration fixes both; real subprocess tests and four-platform two-hop guardian/private-frame tests now pass. Source is frozen for complete-directory regression and independent packaging. No production action or upstream/dependency/schema change.

## Offline closeout and next scope

Final three Python directories passed5456 with37 environment skips; final Web passed671, Svelte/build/format passed, and all147 application Python files match wheel/sdist bytes. Initial unit had2 failures in an obsolete platform assertion; the corrected complete rerun passed. Account-page/deployment wording was also corrected and revalidated. Actual failures, commands, durations and environment boundaries remain in [verification](verification.md); GitHub publication is recorded separately.

This increment is offline-complete, not live login/profile/capture qualification. Continue DY/KS pasted validation, XHS/DY/Tieba creator profiles, KS/Zhihu avatars, remaining media support and real Linux/platform/archive/playback qualification. The original seven-platform goal remains active. This does not restart supervisor or retry the historically failed Bili canary.
