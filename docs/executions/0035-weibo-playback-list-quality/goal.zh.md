[English](goal.md) | **中文**

# 执行 0035 目标

- 状态：冻结的离线 `playback_list` 画质选择范围已完成；真人行保持 `NOT_RUN`
- 日期：2026-09-03
- 前驱：执行 0034 收尾 `3cdd0fc`（推送待网络恢复）
- 范围：`media_info` 携带 `playback_list` 而非 0031 标量 `stream_url` 的微博视频帖，经一个封闭画质偏好解析
- 计划提交：记录于收尾索引；从不嵌入本文件
- 实现提交：记录于收尾索引；从不嵌入本文件

## 结果

1. 为锁定微博 store shim 的视频捕获扩展封闭 `playback_list` 回退：当标量 `stream_url` 缺失时，接受有界的 1–8 项列表，其中每项的 `play_info.url` 通过 0031 封闭 URL 校验器，且可选 `quality` 映射进封闭偏好顺序 `1080p > 720p > 540p > 480p > 360p`；捕获偏好最高的合法项。
2. 未知、缺失或畸形 quality 标签的条目与全部条目不可用的列表不捕获；0031 标量路径保持字节级兼容且优先。
3. 选中的 URL 以不变的私有 `{"url"}` 字段跨越边界，保持同样的防碰撞、递归移除、VIDEO 归一化与逐资产 WB 适配器刷新。
4. 通过真实子进程证明契约组合（标量优先、各偏好层级的列表选择、未知画质与全无效关闭），外加一条仅列表帖的 SQLite → 刷新 → 下载 → Emby 集成重放。

## 验收边界

- 只有封闭嵌套形状与封闭画质偏好授予选择权；URL 后缀、响应 MIME 与下载字节都不能影响它。
- 超过八项的列表、非列表形状、非 Mapping 条目与缺失 `play_info` 不捕获而非静默降级。
- 无数据库 schema 或迁移；稳定 Asset 身份不变。`.upstream` 保持只读且不入库。

## 明确延期

标量+列表混合优先级漂移诊断、杜比/Hi-Res 标签、封面、时长、转发、直播/付费媒体及全部真人验收行均不属于本执行。
