[English](progress.md) | **中文**

# 执行 0035 推进结果

- 状态：冻结的离线 `playback_list` 画质选择范围已实现并通过门禁；真人行保持 `NOT_RUN`
- 日期：2026-09-03
- 计划提交：`ecc08da`（文档基线）

## 已交付

1. `_capture_video` 新增有界封闭 `playback_list` 回退：当标量 `stream_url` 缺失时，以封闭画质偏好 `1080p > 720p > 540p > 480p > 360p` 扫描 1–8 项列表，每个候选 URL 都通过 0031 封闭校验器重校验，捕获偏好最高的合法项；标量路径保持第一且字节级兼容。
2. 未知或缺失画质标签、无效 URL、超界列表与错误形状不捕获；选中的 URL 以不变的私有 `{"url"}` 字段跨越边界，保持同样的归一化与 WB VIDEO 适配器刷新。
3. 覆盖：真实子进程的契约组合（最高画质选择与不可用形状关闭：未知/缺失画质、无效 URL、九项、错误类型），以及集成组合证明 playback 来源帖与标量形状同样完成归一化、下载、归档与 Emby 发布，零工作重放且持久不泄密。

## 验证快照

确切命令、退出码与门禁输出见 [`verification.zh.md`](verification.zh.md)。

## 未完成

杜比/Hi-Res 标签、封面、时长、转发、直播/付费媒体及全部真人验收行继续延期或保持 `NOT_RUN`。
