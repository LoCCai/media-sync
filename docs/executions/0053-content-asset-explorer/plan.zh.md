[English](plan.md) | **中文**

# 执行 0053 计划

- 状态：已完成
- 计划日期：2026-09-05
- 基线：be26cc7
- 数据库 revision：无
- 计划提交：`66e18ff`

## 基线决策

执行 0052 已完成持久操作所有权与观测。执行 0053 交付了下一个可独立产生价值的控制面切片：操作者现在可以查找已采集内容、查看资产完整度、预览不可变归档中已经证明的字节，并提交既有恢复工作流，同时不会获知内部路径或签名 locator。

基线时 API 只有有界数组列表，没有内容/资产详情和归档字节端点；Web 路由会一次拉取 500–1,000 行后在内存筛选，Library 还会展示配置的导出宿主路径。执行 0053 已保留兼容列表形状，把选定筛选移到服务端，新增封闭详情投影，移除宿主路径展示，并保持媒体服务器控制不在本轮范围。

## 交付顺序

1. 记录了已同步的 `be26cc7` 基线、端点/UI 盘点与聚焦测试结果。
2. 新增了与框架无关的 explorer 投影层，提供安全内容/资产摘要和详情，并集中处理官方平台 URL 规范化、有界文本/指标、归档资格和派生允许动作。
3. 新增了 repository 查询，支持精确内容/资产读取、稳定有序关联，以及可选平台、类型、状态、作者、内容、完整度和有界字面文本筛选。
4. 保持了既有列表数组 payload、参数和默认排序，同时增加可选筛选、有界查询文本与 limit、SQL 通配符转义和确定性排序。
5. 新增了精确内容/资产详情端点，返回完整正文文本、有序安全资产、安全时间戳/尺寸/checksum 与聚合导出事实，且不泄漏 ORM/raw/path/locator/错误正文。
6. 新增了只读归档预览服务：验证状态与元数据，要求规范 digest 路径，拒绝链接、可写或多硬链接文件，并由同一描述符完成 hash、seek 和流式读取。在 Windows 上，文件句柄拒绝写入/删除共享；读取要求归档根目录已存在，绝不会创建缺失目录。
7. 新增了严格的完整表示与单 Range 响应，包括精确 200/206/416 响应头、强 ETag 处理、安全 Content-Type 边界、no-store 策略，以及成功、失败、中止和断连时的描述符关闭。
8. 归档缺失、损坏、不安全和未就绪状态现已映射为固定安全错误与既有 asset-download 恢复链接，未修改数据库，也未新增修复 executor。
9. 更新了共享 Web 类型与工具，并升级 Contents：服务端筛选、纯文本详情弹窗及有序资产动作/预览。
10. 升级了 Assets：服务端筛选、安全详情、预览资格、内联/新标签预览，以及既有持久下载/校验恢复提交。
11. 升级了 Library：平台/搜索筛选与作者下钻链接，并移除配置的宿主导出路径。Emby/Jellyfin 树与控制继续明确延期。
12. 完成了安全、路径/Range、API、Web 专项验证、本地 query/弹窗浏览器 smoke，以及 Ruff、format、strict mypy 和 Web 生产门禁。完整 Python 套件结果为 `2456 passed, 3 skipped`；打包、文档、产物与发布证据继续以收尾验证记录为准，不在本计划中推定。

## 冻结契约

- 公开 JSON 只由显式白名单构建；通用 serializer 或 ORM 对象不得跨越 API 边界。
- 搜索是字面、trim、有界并转义通配符；既有列表数组形状和旧筛选继续有效。
- 资产恢复权威保持 POST /api/v1/assets/{asset_id}/download，并产生 0052 已有的持久 asset-download Operation；读取端点绝不 reset Asset 状态。
- 预览身份是 Asset UUID；请求数据不能选择文件系统路径。必须同时满足配置归档根与持久规范元数据。
- 验证与流式读取共享一个描述符；禁止 FileResponse 或任何“校验后重新打开”的路径。
- 必须先验证完整归档表示，再解释 Range。只有 GET 处理一个 ASCII 大小写不敏感的 `bytes` Range；在表示有效时，畸形、多范围和不可满足范围返回 416 与权威总大小。空表示不存在可满足范围。
- HEAD 忽略 Range，以空正文返回已验证完整表示的元数据。`If-Range` 只有与当前强 ETag 精确相等时才激活 GET 范围；过期、弱、日期或畸形 validator 均回退完整 200。
- 安全媒体类型采用 verified probe 输出的封闭集合；未知值回退 application/octet-stream，绝不成为可执行 HTML/SVG。
- UI 只以文本呈现采集正文，绝不渲染 raw HTML，也不呈现或记录 path、locator、source URL、raw record 或异常字段。
- 未新增数据库 migration。

## 验证覆盖

- 投影/repository：稳定排序、可选筛选、百分号/下划线转义搜索、limit 边界、精确 not-found 及禁用字段完全缺失。
- 预览：verified/exported 门、规范路径、SHA/size、同描述符身份、可写/链接/硬链接/越界拒绝、缺失/损坏恢复结果及保证关闭。
- HTTP：已验证完整 GET 200；显式/开放尾部/后缀 GET 206；畸形/多范围/不可满足 GET 416；强 `If-Range`；过期/弱/日期回退；忽略 Range 的 HEAD；精确响应头、空文件/零长度行为与 MIME 回退。
- API 安全：哨兵源 URL、签名 query、本地路径、raw key、validator 与异常正文不得出现在列表/详情/错误/预览响应头或正文。
- Web：筛选构造、安全动作派生、详情呈现及预览/恢复行为，加上 format、单测、Svelte check 和生产构建。
- 外部资格：未使用真实平台账户、创作者 API/CDN、已下载创作者媒体、Linux 持久性演练或真实 Emby/Jellyfin 服务；这些行继续在 Execution 0047 下保持 `NOT_RUN`。

## 交付策略

双语 goal/plan 基线已在实现前以 `66e18ff` 提交；收尾记录继续保留可评审的实现与验证边界。`.mimosa`、`.upstream`、本地数据库、archive/export/job 数据、XML 报告、`node_modules`、`web/build`、`.svelte-kit` 与 `dist` 继续排除在提交之外。
