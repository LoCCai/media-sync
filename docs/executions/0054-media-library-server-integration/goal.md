**English** | [中文](goal.zh.md)

# Execution 0054 goal

- Status: Phase A and Phase B delivered and locally verified; Execution 0054 is complete and live qualification remains `NOT_RUN`
- Started: 2026-09-05 02:45 +08:00
- Predecessor: `22b5864` (Execution 0053 closeout)
- Scope: safe published-library inspection, one environment-owned Emby/Jellyfin profile, durable connection/targeted-refresh operations, bounded exact item lookup, truthful post-refresh item observation, and an evidence-led qualification view
- Phase-B commits: planning `d7e14c9`; implementation/verification `b4af46d`, `ff5da07`, `88f5ed0`, `22bd9ef`, `48ecbe9`, `d8bbdf7`
- Database revision: `0007_media_server_operations`; Phase B adds no migration

## Outcome

Turn the existing deterministic Emby/Jellyfin-compatible export into an operator-verifiable media product. An operator can inspect the current managed author tree and drift without learning host paths, configure exactly one media server outside the browser, test that configured server, trigger a bounded library refresh, look up the exact managed author item, and request a refresh with durable postcondition observation without mistaking acceptance, observation, provider task completion, playback, or mocked checks for one another.

## Acceptance

1. Preserve the existing `GET /api/v1/library` array response and filters. Add an author-UUID detail endpoint whose authority is the database publication chain plus the strict managed manifest, never a caller-supplied path.
2. Return only bounded logical relative nodes and allowlisted publication facts: layout/source/tree/manifest identities, publication Job identity, managed counts, independent freshness and integrity states, protected-user-change state, cursor facts, and fixed allowed actions. `blocked` is a normal freshness state when the current source snapshot is not exportable; it is not mislabeled as publication corruption. Do not return export roots, host paths, raw Job payloads, exception text, locators, source URLs, or file bytes.
3. Inspect manifests and managed files under an existing-only per-author lock and fail closed on malformed manifests, links, path escapes, case collisions, missing files, size/hash drift, replacement races, or inconsistent publication chains. Each request verifies at most 128 files, the operator-configured byte budget (default 1 GiB), and the operator-configured deadline (default 10 seconds), under a process-wide single-flight gate. A manifest-bound cursor prevents pages from mixing different publications. The response reports exact `page`, `complete`, or `budget_exhausted` scope and never upgrades partial work to whole-tree health. Inspection is read-only and performs no repair, deletion, lock creation, or directory creation.
4. Add one immutable environment-owned media-server profile for `emby` or `jellyfin`. The API exposes a hand-built safe summary only; API-key values and complete secret references never cross into API responses, logs, operation payloads, or SQLite.
5. Media-server requests may reach only the configured canonical origin and operator allowlisted IP/CIDR ranges. Requests cannot supply URL, host, key, path, library, or network-policy overrides. Every DNS answer must be allowed, resolution and connection are pinned, redirects are rejected, time/header/body/item limits are enforced, and private/loopback targets require explicit deployment configuration.
6. Persist connection probes and manual targeted refreshes as closed `media-server-probe` and `media-server-scan` Operations. Both endpoints are disabled by default and share one profile-exclusive key; UI action flags never replace server-side enforcement. The strict legacy empty-object `{}` scan remains targetless: only after exact virtual-folder matching does it call fixed `POST /Items/{configured-library-id}/Refresh`, and a trusted 2xx proves acceptance only. `404`, `405`, or `501` fails closed, with no global `/Library/Refresh` fallback and no POST retry.
7. A synchronous lookup API accepts only an author UUID and derives its provider/path selector from the current unique successful publication head plus strict manifest. Emby uses bounded documented filters and Jellyfin uses bounded complete pagination; both require exact local provider and path equality plus proven uniqueness. Only a complete zero-match result is `not_found`; ambiguity, drift, malformed data, or exhausted budgets must not masquerade as absence.
8. The same scan route additionally accepts only strict `{"author_id":"<uuid>"}`. Author mode must first prove an absent baseline and then sends at most one refresh POST. A trusted 2xx persists an accepted checkpoint; only two positively separated observations of the same unique item persist observed and succeed. A matched baseline sends no POST. Post-dispatch acceptance ambiguity becomes non-retryable `media_server_scan_acceptance_unknown`; inability to prove observation after acceptance becomes non-retryable `media_server_scan_completion_unknown` while preserving accepted evidence. This is `post_refresh_item_observation`, not provider task completion or playback evidence.
9. Accepted/observed running checkpoints reuse existing `result_summary` and `operation_phase_changed` vocabulary under lease/revision and cancellation/final CAS fences. Phase-aware restart reconciliation must not overwrite accepted/observed facts; an author scan links its author target and related publication Job. Phase B adds no Operation/Event kind, table, column, or Alembic revision, which remains `0007`.
10. Upgrade Library, Settings, and Jobs to show the real managed tree, safe connection posture, exact lookup, both scan actions, durable state, and qualification schema v2. Author observation shows verification counts but never a remote percentage; unknown-state copy must not reflect server values or imply provider task completion, playback, or automatic retry.
11. Add unit, integration, API, Web, security, migration-compatibility, and packaging coverage. Existing Emby export, content/asset explorer, Operation/Event, legacy `{}` fingerprint/result, migration, and Web contracts remain compatible. Local export success, trusted acceptance, observed postconditions, and mocked tests must not imply a live PASS.

## Qualification boundary

This execution proves local manifest/tree behavior and fake or mocked Emby/Jellyfin protocol behavior only. No authorized real server was used in this workspace. Qualification schema version 2 reports `connection_probe`, `library_discovery`, `targeted_scan_acceptance`, `item_lookup`, and `post_refresh_item_observation` as implementation `IMPLEMENTED` with human `NOT_RUN`. `provider_task_completion` is `NOT_IMPLEMENTED` with reason `provider_api_unsupported`; `playback_evidence` and `automatic_post_export_scan` are also `NOT_IMPLEMENTED`. Unimplemented capabilities have null human status. Linux deployment evidence and every seven-platform live-account/CDN row remain `NOT_RUN`.

## Deferred

Execution 0054 has no remaining local implementation item. Real lookup and post-refresh observation may be qualified later under execution 0047. Provider task completion requires a future provider-specific correlated task contract and is not approximated. Playback-evidence mutation waits for operator authentication and remains 0055 work. Browser-writable settings, multiple profiles, destructive cleanup, retention, orphan repair, forced overwrite, and access control also remain 0055 work; final legacy-console removal remains 0056 work. Automatic post-export scanning is future scope whose execution assignment has not been frozen.
