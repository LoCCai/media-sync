[English](plan.md) | **中文**

# 执行 0050 计划

- 状态：离线基础已执行并验证
- 计划日期：2026-09-04
- 前驱：`6d68768`
- 数据库迁移：无
- 实现与收尾提交：包含本记录的提交（不嵌入自身 SHA）

## 基线

操作者首次容器运行证明 Chromium 本身可启动，但 `mediacrawler doctor` 在 `license_digest_mismatch` 停止；旧构建清单同时记录 `chromium: launch-failed`。单文件控制台把全部工作流铺在一个长页面上，每次刷新后都要求两个勾选，且缺少分页面任务上下文。因此 0050 把可跨平台资格修复与可独立移除的 SPA 基础一并交付，同时保留全部后端安全门禁。

## 交付顺序

1. 以规范化 LF 语义修正并测试针对锁定上游 blob 的 LICENSE 资格检查。
2. 建立前端工具链、设计 token、壳层、响应式导航、共享控件、API 客户端与类型化投影。
3. 先迁移总览与账户/订阅/任务/资产只读面，再接通已有创建/登录/运行/下载/导出操作。
4. 新增内容与媒体库投影端点、诊断、设置、一次性确认和 `/legacy` 回退路径。
5. 集成静态服务、路由回退、缓存/安全响应头与最终不含 Node 的 Docker 多阶段构建。
6. 验证浏览器行为、参考风格一致性、桌面/手机布局、静态资源打包及 Python/前端质量门。
7. 更新部署/状态/日志记录，提交全部预期文件，自动推送 `main` 并核对本地/远端 SHA。

## 验证计划

- 前端：Prettier、`svelte-check`、Vitest 与 adapter-static 生产构建。
- 后端：完整 API 测试、checkout/许可证专项契约、Ruff、格式、strict mypy、compileall、包构建、文档和上游锁。
- 浏览器：每个路由都有有效主体，无框架错误层或控制台错误；执行重置 → 首次确认 → 刷新持久化；把桌面和手机截图与 bili-sync 参考图直接检查。
- Python 完整套件：运行并如实记录 Windows 结果，不压掉既有 receipt/进程抖动。
- Docker：本站静态审计 Dockerfile/上下文；实际镜像构建、清单与运行时探针由操作者 Linux 主机执行。

## 提交策略

全部实现、测试和收尾记录在上述门禁后一起提交。按操作者要求，结果提交自动推送到 `origin/main`；生成的前端产物、`node_modules`、本地数据库和原始 junit XML继续保持忽略。
