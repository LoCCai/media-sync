[English](progress.md) | **中文**

# 推进记录

上一目标轮完成了0056代码发布和远端一致性证据，分类为进展；本轮按现有源码继续自动资料查询。当前基线 `87ef7fd` 工作区干净。已核对上游单资料接口、共享Author覆盖风险及现有Operation边界，冻结上述计划。

计划先提交为 36e004d，然后并行实施。Bili saved-session 首片源码与界面已实现，本地验证完成，发布等待收尾。离线测试不构成真人平台通过。

## 已实现

- 独立guardian监管来源验证、Chromium和一次单资料接口；认证/WBI有界，不调用内容/动态/评论或扫码回退。共同账户锁覆盖完整子树，未知清理保留锁和固定事件。
- 0010增加账户隔离资料/查询表、认证单调修订和订阅备注；保留旧作者名称/URL、归档路径、检查点和历史。并发创建使用insert-if-missing，后到的账户不能覆盖既有作者。
- 双重租约/取消栅栏保护短事务，最终资料与Operation成功摘要原子提交。第二栅栏定义成功时刻，终态前刷新ORM修订。凭单绑定精确操作/账户/作者/版本/期限，读取错误采用固定白名单。
- 头像独立十秒进程执行受限公网请求/解码，禁重定向/环境代理，限制两MiB、八百万像素、单帧，重编码无原始元数据PNG存数据库。新增锁定Pillow依赖，CSP不变，头像失败保留旧版本。
- 输入完成每身份代际查询一次，昵称不覆盖本地备注；有效成功凭单允许不填备注创建。列表/详情展示名字、头像、观察时间。浏览器发现许可证未确认时提示含糊，已修为明确“未发起查询”，不消耗自动机会、不代确认许可证。

## 仍需完成

完整离线检查点已结束：3972通过、22跳过、1警告，852.47秒；最终加固后的冻结源码联合回归575项通过，109.47秒。两次运行覆盖重叠，不冒充最终源码第二次完整运行。Web最终19文件492项测试与check/format/build通过。静态、打包检查及发布状态见[验证记录](verification.zh.md)。

浏览器仅做正常测试登录、仅浏览和预置合成资料检查，没有代确认许可证或在UI发起资料查询/创建；独立API测试覆盖写入闭环。控制器测试覆盖门禁文案修正，其浏览器复验尚未完成。专用标签和核实身份后的临时服务均已关闭。

其他六平台和Cookie资料、粘贴Cookie校验/私密保存/复用、正确有界B站覆盖、真实采集/归档/媒体服务器资格仍开放。没有生产部署或恢复supervisor，历史B站零内容失败未重新验收。
