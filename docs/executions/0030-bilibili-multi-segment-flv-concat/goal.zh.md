[English](goal.md) | **中文**

# 执行 0030 目标

- 状态：冻结的离线多段 FLV 拼接范围已完成；真人行保持 `NOT_RUN`
- 日期：2026-09-02
- 前驱：执行 0029 收尾 `dbd06075eac67377a911b503de9aa609fdc30c79`
- 范围：有界、有序的 Bilibili 多段 progressive `durl`（按封闭顶层格式分类为 FLV），每段结构探测为 FLV，并经一次固定参数 stream-copy ffmpeg 拼接为一个 Emby 兼容 MP4
- 计划提交：记录于收尾索引；从不嵌入本文件
- 实现提交：记录于收尾索引；从不嵌入本文件

## 结果

1. 把严格详情进程协议升级到 v9，多段 FLV 授权只来自封闭顶层格式分类加有界 2–64 `durl` 元组；精确一段 FLV、多段普通与 DASH 行为保持字节级兼容。
2. 通过既有 `{"cid", "segments"}` 私有桥接加精确 `"format": "flv"` 标记，携带一个 repr-safe 瞬态多段 FLV 目标；严格防碰撞、持久化前递归移除，且仅当 payload CID 与所选分 P 匹配时重建。
3. 在共享字节上限与截止时间下按序把每个分段下载到 generation-scoped 分段 store，复用既有候选轮次故障切换、严格续传、整轮 restart 与一次必须返回同类型同分段数的全鉴权刷新。
4. 要求每个完成分段结构探测精确为 FLV 视频，再用固定参数、有界的 concat-demuxer `ffmpeg -c copy` 单次调用拼接全部分段、保留首个视频流与可选首个音频流，且只发布精确探测为 MP4 的成品。
5. 拼接/成品门失败后保留各分段可续传状态，阻止未完成成品进入恢复路径，保留已发布成品恢复，显式清理时移除全部分段状态，且绝不发布任何原始 FLV 字节。
6. 以一次真实的本地双段 FLV → 拼接 → Emby 组合证明主地址失败、有序备用、零工作重放与留存树不泄密，同时全部真人账户/API/CDN/媒体服务器行保持 `NOT_RUN`。

## 验收边界

- 精确一段 FLV 继续走 0027 转封装路径，多段普通继续走 0029 拼接路径，本执行只新增有界 2–64 多段 FLV 形状。
- 多段 FLV 授权只来自封闭 `durl` 元组形状加封闭顶层格式分类；URL 后缀、响应 MIME 与下载字节都不能授予该授权。
- 每个分段必须精确探测为 `video/x-flv`；混合或非 FLV 分段一律关闭失败不发布，成品必须精确探测为 `video/mp4`。
- 每个 generation 一次 ffmpeg 调用、固定 argv、非 shell、输出/超时/共享媒体字节上限有界；concat 列表文件只存在于受控 parts 目录内，且不跨越尝试存活。
- 无数据库 schema 或迁移；稳定 Asset 身份与冻结媒体形状计数不变。`.upstream` 保持只读且不入库。

## 明确延期

转码、编解码修复、FLV 分段字节级预拼接、CDN 排序/竞速/跨运行缓存、混合/非鉴权穷尽刷新、字幕/弹幕、超过 64 个分 P、番剧/付费/直播媒体、更广平台形状、REST/生产打包及全部真人验收行均不属于本执行。
