**English** | [中文](progress.zh.md)

# Progress

Fresh baseline verified. Previous turn classified as progress, not a wait or completion. Parallel source audits found publicly reachable fixed historical protocol evidence that changes the next action: dynamic detail and complete OPUS can now be implemented without inventing endpoint/shape contracts. Current shutdown HEADs remain non-evidence. Locked runtime repositories, licenses and dependency files are unchanged; only protocol facts will be implemented originally. See [source evidence](sources.md).

Remaining-platform audit also found a later Tieba self-info candidate in aiotieba, plus KS single-creator field declarations and historical Zhihu avatar fields. These are documented for future increments, not silently promoted to verified login/profile support or substituted for the dynamic workflow now made actionable. No implementation or new test PASS at plan freeze.

## Local implementation in progress

Wired explicit policy v2 scope, composite cursor, independent dynamic head/history lanes, private immutable whole-page snapshots, zero-record discovery units, exact WORD/DRAW/OPUS and owned AV references, sealed coverage/normalization/CLI/scheduler/atomic ingestion. Original policy v1/upload cursor semantics remain unchanged. Both dynamic lanes conservatively traverse to observed source end without assuming timestamp order; cycle detection retains only the most recent64 offset hashes. No infinite-history or completeness claim.

Creation UI/API/CLI now expose scope. Existing scope edits require paused, idle subscriptions and the current schedule revision, preserve both feed checkpoints/media, and do not resume work. Unpublished transactions recheck current scope; exact succeeded-Run readback precedes later policy/item-limit changes. Fixed unsupported/identity/schema child outcomes reach scheduler diagnostics without body, Cookie or image URLs.

Independent review reproduced succeeded-Run replay rejection after scope/item-limit edits; validation was reordered and regression added. Refresh review found missing OPUS preflight, repeated signing-key requests and lost unsupported classification; corrections and tests are ongoing. This is an implementation snapshot, not final-source or production completion. No production accounts/subscriptions/capture/download/export/deployment/supervisor actions.

## Frozen implementation and review closeout

The preceding in-progress paragraph is superseded: review corrections are complete and application sources are frozen for final affected regression. Explicit v1/None scope is now fenced both at CLI loading and within ingestion, including zero-record artifacts and changes between loading and Run creation. Exact succeeded-Run replay remains zero-write after later scope/item-limit changes. Historical failed_retryable Runs without active Jobs do not block a paused scope edit; executing work still does.

Snapshot crash recovery now writes a complete private same-directory temporary file, flushes/fsyncs, then atomically publishes without replacement and fsyncs POSIX directories. Subprocess `os._exit(93)` tests cover partial write, pre-publish and post-publish death. Other crashed temporary files are not adopted/deleted; bad existing final blobs are never overwritten. No GC was added. Windows native publication was exercised; Linux/macOS native primitives remain unexecuted here.

Dynamic source/attachment contract failures are terminal for the Job without treating them as authentication faults or account-circuit evidence. Exact refreshed image identity is checked before HTTP, while permitted CDN/signature rotation preserves stable identity. Real offline SecureMediaDownloader → archive → EmbyExportService checks cover WORD/two-image DRAW and repeated download/export without a server connection. These are synthetic-byte/local-output checks, not platform/CDN or native gallery playback qualification.

Roadmap and capability documentation now separate locked-upstream facts from current0056–0062 product behavior. Final counts, packaging and publication are recorded in verification; the overall seven-platform goal and the unresolved live canary remain open.

Final frozen-source affected regression completed:1250 passed,15 environment skips,1 existing warning/340.04s. Web642 and all available static/build/docs gates passed. This closes the0062 offline implementation increment; publication is tracked below in verification. No runtime source changed after final freeze.
