[English](progress.md) | **中文**

# 推进记录

新基线已核验，上一轮归类为progress，不是等待或完成。并行源审计找到了公开可达固定历史协议证据，改变后续动作：动态详情及完整OPUS不再需要猜端点/形状。当前关停HEAD仍不作为协议证据。锁定运行上游、许可证及依赖文件不变，只按协议事实原创实现，见[来源证据](sources.zh.md)。

其他平台审计还找到aiotieba本人信息候选、KS单作者字段声明及历史知乎头像字段，作为后续增量资料，不静默提升成已验证登录/资料支持，也不替代现在已具备依据的动态闭环。计划冻结时尚无实现或新测试PASS。

## 实施中的本地进展

已接入显式policy v2范围、复合cursor、独立动态head/history、私密整页snapshot、零内容发现单元、精确WORD/DRAW/OPUS及自有AV引用，以及sealed coverage/normalizer/CLI/scheduler/原子入库。原policy v1/投稿cursor不变。动态两通道均保守遍历至本轮源末尾，不根据未证明有序的时间戳提前跳页；循环检查限最近64个offset哈希，不声明无限历史或完整覆盖。

订阅创建UI/API/CLI增加范围选择；已有订阅只能在暂停、空闲、日程修订匹配时显式修改，保留两feed断点与媒体，保存后不自动恢复。未发布单元事务核对当前scope；已成功Run精确回读优先于之后变更的策略与数量限制。固定动态不支持/身份漂移/结构错误经child退出码进入scheduler中文诊断，不输出正文、Cookie或图片URL。

独立审查发现成功Run在切范围/缩小数量后回读失败，已调整验证顺序并补回归；图片刷新另发现OPUS前置检查不足、重复签名密钥请求和unsupported分类丢失，正在修正与补测。这是实施快照，不是最终源码或生产完成声明。未操作线上账户、订阅、采集、下载、导出、部署或supervisor。

## 实现冻结与审查收口

此处更新上段实施中状态：审查修正已完成，应用源码冻结，进入最终受影响回归。CLI加载及入库事务都显式约束v1/None范围，包括零内容artifact，以及加载后、创建Run前发生范围变化；成功Run在之后切范围/改数量仍精确零写回读。无活跃Job的历史failed_retryable Run不再阻止暂停订阅改范围，正在执行的工作仍拒绝。

快照崩溃恢复改为同目录私密临时文件完整写入、flush/fsync后原子无替换发布，再fsync POSIX目录。真实子进程 `os._exit(93)` 覆盖写到一半、发布前与发布后退出；不接管/删除其他崩溃临时文件，不覆盖已有损坏终态文件。未新增自动GC。Windows原生发布已执行；Linux/macOS原语在本机未执行。

动态源/附件契约错误终止该Job，不作为认证错误或账户熔断证据。精确图片身份在HTTP前核对，允许不改变稳定身份的CDN/签名更新。真实离线SecureMediaDownloader → archive → EmbyExportService覆盖WORD/双图DRAW及重复下载/导出，无需服务器连接；这是合成字节/本地输出验证，不是平台/CDN或原生图集播放验收。

路线图与能力矩阵已区分锁定上游事实和0056–0062产品现状。最终计数、打包及发布见验证文档；七平台总目标与未解决的线上canary继续开放。

最终冻结源码受影响回归完成：1250通过、15项环境跳过、1项既有警告/340.04s；Web642及全部可用静态/构建/文档门禁通过。0062离线实现增量至此收口，发布结果在验证文档另记。最终冻结后未再修改运行源码。
