[English](plan.md) | **中文**

# 执行 0049 计划

- 状态：已执行并验证
- 计划日期：2026-09-03
- 前驱：`0eb3f895b02137cbfe231c705ba34aa1ce86a9f4`
- 数据库迁移：无计划
- 计划提交：`dcba270dfc5f16429aec356e5b521feb4c7ec2e3`
- 实现提交：`52f975672449266cb4f68c5f6e4f846a27e370eb`

## 基线与审计

RC 前置评审认可 0048 的方向，但拒绝把 `0eb3f89` 作为 Linux RC 基线。两个静态即可确认的 Docker 阻塞（checkout 位于 `/opt/mediacrawler` 而锁文件相对 `/app` 解析为 `.upstream/MediaCrawler`，且 `.git` 被删除；Playwright 以 root 安装进运行用户无法读取的按用户缓存）、两个操作语义问题（失败或受阻的下载仍以 `succeeded` 收尾；「重下载」标签夸大了已验证资产的无操作语义）、缺失的正向生命周期 API 测试、整份重复且携带过时索引的项目日志文档对，以及若干状态/架构/声明残留。

实现前记录的基线门禁：拉取后的树在本工作站通过 `ruff check .`、文档链接检查（416 文件）与上游锁检查；新增 0039/0040 单元文件通过（14 项）。

## 交付顺序

1. Dockerfile：把锁定 checkout 克隆到 `/app/.upstream/MediaCrawler` 并保留 `.git`；安装与运行两个阶段均设 `PLAYWRIGHT_BROWSERS_PATH=/opt/ms-playwright`；浏览器缓存 chown 给运行用户；构建清单记录实际启动的 Chromium 版本；doctor 预检保持为阶段 B 门。
2. Compose/部署：记录 digest 钉版基底镜像构建、把 `BASE_IMAGE` 作为 build arg 透传、注明 bind mount 必须同时应用到两个服务。
3. API：失败/受阻的下载操作以 `error_code` 收尾；端点/控制台措辞改为下载/校验；全部后台线程与登录状态路径改用应用捕获的设置。
4. 测试：补充真实 Asset 生命周期覆盖（succeeded、blocked→failed、已验证无操作）并保持既有门禁绿色。
5. 父进程诊断：把固定完成回执原因码带入脱敏消息；把编写工作站失败清单捕获为脱敏的 junit 派生工件。
6. 文档：两份日志文档去重、同步 0043/0044/0047 索引行与 0043/0044 计划状态、0044 以被吸收方式关闭、修正架构/第三方声明/状态措辞、强化文档检查器（重复 H1/H2、游离切换器、中英标题结构）。
7. 运行专项与完整离线套件及全部静态门；撰写四份执行文档；创建双语实现/收尾提交，推送并对账。

## 计划提交顺序

1. 文档基线
2. 实现
3. 文档收尾

上游树保持排除、未修改且干净。
