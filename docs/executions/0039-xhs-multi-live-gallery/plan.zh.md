[English](plan.md) | **中文**

# 执行 0039 计划

- 状态：已执行并验证
- 计划日期：2026-09-03
- 前驱：`064bdb1d4ab493ec2b31afb96a29032a8b939b2d`
- 数据库迁移：无计划
- 计划提交：记录于收尾索引；从不嵌入本文件
- 实现提交：记录于收尾索引；从不嵌入本文件

## 基线与审计

执行 0038 在 `064bdb1` 干净、已推送并对账。实况 shim 只捕获恰一张图的形状；多图实况 note 不捕获并回退。store 保留逗号拼接的图片 URL，因此 shim 只需要逐图实况元组。

实现前记录的基线门禁：0038 专项 `355 passed in 6.92s`、detail 契约 `116 passed in 80.18s`、完整 `2032 passed, 1 skipped in 371.84s`、Ruff/格式干净、strict mypy 干净、docs（336 文件）与 upstream（2 个锁定 checkout）通过。

## 交付顺序

1. 为 `xhs_live.py` 扩展 v2 列表 payload（2–16 个逐图实况 URL，全有或全无）至一个新的严格防碰撞私有字段。
2. 为 `_normalize_xhs` 扩展成对 gallery 分支（相等有序元组、MIXED、空 `video_url`）并把字段加入递归移除集合。
3. 扩展 creator 回退的 `normal` 类型分支以绑定精确成对 gallery 形状。
4. 为捕获矩阵（2/16 成对捕获、部分/超界/畸形不捕获）补充单元/契约覆盖，为多资产 下载 → 归档 → Emby 零工作重放组合补充集成覆盖。
5. 运行专项与完整套件及全部质量门家族；更新四份执行文档与根事实，然后创建双语实现/收尾提交，推送并对账 GitHub。

## 计划提交顺序

1. 文档基线
2. 实现
3. 文档收尾

`.upstream` 保持排除、未修改且干净。
