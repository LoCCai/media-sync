**English** | [中文](status.zh.md)

# Unified project status (single source of truth)

The pushed execution 0055 backend-authentication implementation is commit `f19bfaa` (the frozen planning baseline is `4564b2a`). It resolves a required typed operator credential before bind, supports an optional distinct Bearer credential, enforces exact Host/Origin policy, rotates a process-local HttpOnly `SameSite=Strict` session cookie, requires CSRF on unsafe cookie-authenticated methods, applies deny-by-default ASGI protection with an exact anonymous allowlist, accepts only strict bounded login JSON, and wires the credential/origin contract into Docker Compose. Its 190-test auth/API focus and complete offline regression (`2811 passed, 14 skipped, 1 warning in 561.43s`) pass, as do 69 Web tests and all locally available static/build/docs/package gates. Three skips are Windows/POSIX differences; 11 real-PostgreSQL races and Docker validation were unavailable on this workstation and are not claimed.

The pushed observation-identity and persistent-ledger implementation is commit `1d5b448`, with its publication record at `f27d467`. This confirmation checkpoint connects that foundation to a browser-session-only playback-confirmation backend: `PlaybackEvidenceService` re-resolves the publication, performs one complete unique lookup, resolves it again, compares the current identity in constant time, releases its bounded authority lock, and only then opens the short create-or-replay transaction. `POST /api/v1/media-server/playback-evidence` accepts only the canonical author UUID plus opaque observation fingerprint, requires the exact browser cookie/Origin/CSRF authority, rejects Bearer-only and mixed Cookie/Authorization requests before handler work, rejects `Idempotency-Key`, and returns only the six-field safe created/replayed projection. Focused service, SQLite-composition, API-contract, authentication and media-server regressions pass; after closing the Alembic isolation and default-serve audit-visibility defects, the complete suite passes `2941 passed, 22 skipped, 1 warning in 594.72s`. Real PostgreSQL and Docker remain `NOT_RUN` on this workstation. The safe bounded by-author current/stale evidence projection and qualification schema v3 do not exist yet. `GET /api/v1/qualifications` therefore remains schema v2 and truthfully reports `playback_evidence` as `NOT_IMPLEMENTED` with `human_status: null`; Console v2 and `/legacy` also still lack login/session/CSRF integration and confirmation UI. Live playback and every execution 0047 live qualification row remain `NOT_RUN`. Provider task completion remains unsupported by the common APIs and is not a 0054-B or 0055-A claim. Execution 0047 remains the open P0 operator gate; absent capabilities are `NOT_IMPLEMENTED`, not unexecuted live rows. This table is the canonical status view; per-execution detail lives in [`executions/`](README.md) and evidence in each verification record. Update this page at every closeout.

## Milestone status

| Milestone | Status |
| --- | --- |
| Offline feature work | Platform shapes frozen at the 0039 boundary plus 0040/0044 operations and the 0050 Console v2 control-plane foundation; 0043 (danmaku/subtitles) remains deferred |
| REST API + web console | 0050–0053 foundations plus execution 0054 safe managed-tree paging, redacted media-server posture, durable acceptance-only and author-observation Operations, exact item lookup, qualification schema v2, and truthful Library/Settings/Jobs evidence are delivered; commit `f19bfaa` implements the 0055-A backend authentication boundary, and the current checkpoint implements the browser-only playback-confirmation POST. Web login/session/CSRF integration, by-author evidence projection and confirmation UI remain pending, so the console is not currently an operable administration surface |
| Operator authentication + playback evidence | Commit `f19bfaa` implements the backend single-operator boundary; `1d5b448` implements matched-only identity, revision `0008` and repository replay/concurrency primitives; the current checkpoint implements and offline-verifies the TOCTOU-safe browser-only confirmation service/API. Safe current/stale projection, qualification schema v3 and Web integration remain pending. The schema-v2 qualification capability is still `NOT_IMPLEMENTED` with null human status; live playback is `NOT_RUN` |
| Docker packaging | Candidate image plus reproducibility hardening and the Node-free multi-stage Console v2 build delivered (0041, 0048–0050); the repaired operator image built/started with green doctor, deep readiness and Chromium launch |
| Operations docs / security review / release checklist | Delivered (0045, 0046) |
| Live qualification (final gate) | Open — execution 0047, operator-assisted on Linux |

## Verification matrix

| Dimension | State | Evidence / blocker |
| --- | --- | --- |
| Implementation (offline shapes) | 15+ frozen shapes across seven platforms | Executions 0013–0039 records |
| Offline complete suite | Current confirmation checkpoint: `2941 passed, 22 skipped, 1 warning in 594.72s`; 3 skips are Windows/POSIX differences, 11 are Operation PostgreSQL races and 8 are PlaybackEvidence PostgreSQL races without a configured URL | Current execution 0055 verification; execution 0054 remains the latest real-PostgreSQL comparison |
| API/console tests | Committed 0055 auth/config/seven-API focus passes `190 passed, 1 warning in 41.99s`; the current browser-only confirmation service/API, strict request/OpenAPI, mixed-credential pre-handler rejection and SQLite composition regressions pass. The last Web suite passes 69 tests plus format/check/build, but no Web login/session/CSRF implementation, confirmation UI or authenticated browser smoke is claimed | Execution 0055-A verification and current confirmation checkpoint |
| Static gates (ruff/format/mypy/compileall/docs/frontend check/build) | Current checkpoint passes Ruff/format across 731 files, strict mypy across 106 source files, compileall, 498-document links, both clean pinned upstreams, Web 69 tests plus format/check/build, and isolated 124-entry wheel/810-entry sdist inspection with `.mimosa/` excluded | Execution 0055-A verification and current confirmation checkpoint |
| Docker image build | The 0050/0047 image preflight remains historical PASS. Current 0055 auth-aware Compose wiring is code-reviewed but `NOT_RUN` because Docker CLI is unavailable on this workstation | Executions 0050/0047 and 0055-A verification |
| Container readiness / restart persistence / backup-restore drill | Deep readiness `PASS`; restart persistence and backup/restore `NOT_RUN` | Execution 0047; docs/operations.md procedures ready |
| Live login (any platform) | `NOT_RUN` — operator (Phase C canary: Bilibili + XHS) | Execution 0047 |
| Live crawl / download / incrementality | `NOT_RUN` — operator (Phases C–E) | Execution 0047 |
| Real Emby/Jellyfin connection, Library discovery and targeted-refresh acceptance | `NOT_RUN` — implemented in 0054-A but no authorized server was used | Executions 0054 and 0047 |
| Provider/path item lookup and post-refresh item observation | `IMPLEMENTED / NOT_RUN` — local/mock gates pass, but no authorized real Emby/Jellyfin server was used | Execution 0054-B verification |
| Provider task completion | `NOT_IMPLEMENTED` — the common Emby/Jellyfin refresh APIs provide no durable task identity; phase B does not claim it | Execution 0054-B truth boundary |
| Operator access control | `IMPLEMENTED / offline verification PASS` — the backend fails before bind without its required typed credential, enforces exact Host/Origin plus deny-by-default route protection, and supports a rotating HttpOnly session with CSRF plus an optional distinct Bearer credential. The 190-test focus and complete locally available suite pass. Web login/session/CSRF integration remains pending, so no operable authenticated console or live qualification is claimed | Execution 0055-A [goal](executions/0055-operator-auth-playback-evidence/goal.md) and [verification](executions/0055-operator-auth-playback-evidence/verification.md); implementation commit `f19bfaa` |
| Playback-evidence identity / persistence / confirmation backend | `IMPLEMENTED / offline verification PASS, not yet the qualification capability` — matched-only observation identity, revision `0008`, exact create-or-replay persistence, TOCTOU-safe confirmation service and browser-only POST exist. SQLite composition passes; real PostgreSQL races remain `NOT_RUN` and do not prove full PostgreSQL support | Current execution 0055 confirmation checkpoint |
| Playback-evidence projection and qualification / automatic post-export scan | `NOT_IMPLEMENTED` — there is no bounded by-author current/stale projection or qualification schema v3, so schema v2 continues to report `playback_evidence` as unimplemented with null human status despite the safe write backend. Web confirmation is also absent. Automatic chaining remains excluded from phase A and has no frozen assignment | Execution 0055-A [plan](executions/0055-operator-auth-playback-evidence/plan.md) and execution 0054 qualification boundary |
| External security audit | `NOT_RUN` — optional | docs/security-review.md residual risks |

## Release blockers (v0.1.0-rc1)

1. Execution 0055 is incomplete: bounded by-author current/stale evidence projection, qualification schema v3, Web login/session/CSRF integration, confirmation UI and their closeout verification remain open.
2. Linux-host baseline incomplete (complete suite, host-port review, restart persistence, backup-restore and process baseline) — Phase B.
3. Zero live rows recorded — Phase C canary (Bilibili + XHS) then remaining platforms.

Minimum 0.1 release condition: at least the two canary platforms reach **Supported** (login, sync, download, true incrementality, Emby rescan + sample playback), every other platform honestly classified (Experimental / Metadata-only / Blocked External / Unsupported), and the project describes itself as "seven-platform adapter framework; see the status matrix" rather than "supports seven platforms".
