[English](upstream-replication-review.md) | **中文**

# 上游复刻完成度审查（归档，执行 0042）

原始计划：把 MediaCrawler 的平台爬取能力与 bili-sync-up 的归档/Emby/Web 工作流复刻为一个自托管服务，并在 media-sync 自有的许可证安全边界内结合两者。本文档逐能力归档：已交付（附证明它的执行记录）、明确延期、以及在操作者协助终验（执行 0047）之前保持 `NOT_RUN` 的部分。状态边界：`main` 上执行 0041。

## 1. 对照 MediaCrawler（爬虫侧）

| 能力 | 状态 | 证据 |
| --- | --- | --- |
| 七平台账户模型（`xhs`/`dy`/`ks`/`bili`/`wb`/`tieba`/`zhihu`） | 已交付 | 执行 0003 schema；0004 桥接 |
| 二维码 / Cookie / 保存会话登录与受 fencing 保护的状态机 | 已交付（离线范围） | 执行 0011、0012；真人行 `NOT_RUN` → 0047 |
| 作者订阅抓取（creator 模式）+ 许可证门禁外部进程 | 已交付（离线范围） | 执行 0004、0007、0008 |
| 作者权限解析（secret 引用、有界查找） | 已交付（离线范围） | 执行 0017、0019、0020 |
| 逐平台内容发现 | 已交付：七平台 15+ 冻结形状 | 执行 0013–0024、0031–0039 |
| 增量状态（cursor + 水位 + 已知 ID） | 已交付——超过上游（上游没有） | 架构 §5；执行 0003 |
| 进程隔离、机密引用、脱敏 | 已交付——超过上游（上游把 Cookie 放命令行） | 执行 0004；ADR-0001 |
| 评论采集 | 明确非目标（SAFE-002 默认关闭） | 需求 §0.x 非目标 |
| 关键词/搜索抓取 | 明确非目标（纯订阅制产品） | 需求 §0.x 非目标 |
| 手机号登录 | 不开放（上游枚举宽于实际） | 执行 0012 真实性边界 |
| 真人登录/抓取验收 | `NOT_RUN`——操作者，执行 0047 | roadmap 阶段 5 |

## 2. 对照 bili-sync-up（归档 / Emby / Web 侧）

| 能力 | 状态 | 证据 |
| --- | --- | --- |
| 持久可续传下载（`.part`、Range 续传、restart fencing） | 已交付——超过上游（原子、带校验和） | 执行 0005、0025–0026 |
| Bilibili DASH 选择 + stream-copy 合并 | 已交付 | 执行 0024 |
| 多段 `durl` 拼接 / 多分 P FLV | 已交付 | 执行 0029、0030 |
| CDN 备用故障切换（有序、有界） | 已交付：DASH + progressive + FLV | 执行 0025、0026、0027 |
| 确定性 SHA-256 归档 | 已交付 | 执行 0005 |
| Emby/Jellyfin NFO 媒体库（剧集/季/集、海报、图集） | 已交付（layout v1） | 执行 0005、0016 |
| 弹幕（ASS）/ 字幕 sidecar | 延期 → 执行 0043 | roadmap 阶段 3 延期行 |
| 定时自动同步 → 下载 → 导出 | 已交付（持久调度 + 常驻监督器） | 执行 0006、0010、0012 |
| Web 管理 | 部分：本地 REST API + 中文控制台已交付 | 执行 0040；强化 → 0044 |
| UI 任务队列可视化 | 部分（jobs + operations 端点）→ 0044 | 执行 0040 |
| 凭据/配置管理 UI | 未建（机密引用保持 CLI/secret-store 管理） | 有意边界 |
| 番剧/动漫、多用户、公网部署 | 明确非目标 | 需求 §0.x 非目标 |

## 3. 超出两个上游的横向交付

- 文档审计链：47 个执行、每执行双语四件套，真实性规则强制执行（`NOT_RUN` 绝不冒充通过）。
- 本地 REST API + 内置 Web 控制台与 QR 中继（执行 0040）；Docker 打包含锁定上游、镜像源与 Xvfb（执行 0041）——操作者验证在 Linux 进行，不在本机。
- 有界质量门：严格 mypy、Ruff/format、compileall、文档链接检查；离线完整套件最近记录为 2032 通过 + 1 项 Windows 不适用跳过（执行 0039 基线）——更新的测试（0039–0040）在部署主机运行。

## 4. 到最终门的剩余序列

| 执行 | 范围 | 验证模型 |
| --- | --- | --- |
| 0043 | Bilibili 弹幕/字幕 sidecar（离线） | 静态 + 离线套件（Linux 主机），真人行 `NOT_RUN` |
| 0044 | 控制台/REST 运维强化 | 静态 + 离线套件（Linux 主机），真人行 `NOT_RUN` |
| 0045 | 运维备份恢复 + 升级文档 | 仅文档门 |
| 0046 | 安全与隐私审查 + 发布清单 | 仅文档门 |
| 0047 | 七平台真人验收（最终门） | 操作者在 Linux 协助；逐行如实记录 |

文档永远不会让真人行通过。执行 0047 是最后一个里程碑：只有操作者逐平台记录真实结果后，原始合并计划才算完成。
